# 2026-05-21

## Codex startup warnings cleanup

- Replaced repo-local `.codex/config.toml` `[features].codex_hooks = true` with the canonical `hooks = true`.
- Shortened invalid skill frontmatter descriptions for `overleaf-paper-sync`, `social-scraping-policy`, and `win98-paper-theme`; `win98-paper-theme` now uses a folded YAML scalar to avoid colon parsing errors.
- Ran `make sync`, regenerating `personal-site/lib/skills.generated.json` and the three public skill-file copies.
- Added a reversible `~/.codex/config.toml` curation block named `bingran-you Codex skill budget curation`. It disables overflow skill discovery entries, especially nested vendored Xiaohongshu subskills, while keeping the source files and website catalog intact. Remove that block to expose all repo skills to Codex startup again.
