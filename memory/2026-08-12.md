# 2026-08-12

## Scheduled sweep — 2 submodule bumps + 0 posts (PR #545)

### Submodule bumps

| Submodule | Old | New | Change |
| --- | --- | --- | --- |
| `current-projects/first-tree` (main) | `19e66032a` | `35a5266c2` | 2 commits — defer GitHub Issue Task Agent until first comment (#2287), restore tool-agnostic QA workflow (#2289) |
| `trusted-external-repos/open-design` (main) | `eefe7967` | `d822cdc41` | 13 commits — daemon auth for Docker browser peers (#6715), Docker runtime CVE floors (#6733), MCP slash-command docs (#6597), Launch Week hero fix (#6766), artifact upgrade gate scope (#6760), duplicate queued chat guard (#6749), stale run recovery card hide (#6748), workspace authority cache safety (#6752), missing vela runtime unavailable (#6738), plus 4 more polish fixes |

Neither bump touches any `skills/` path (`git diff --name-only ... -- 'skills/'` empty for both), so `make sync` produced zero diff to `.agents/skills`, `.claude/skills`, `personal-site/lib/skills.generated.json`, or `personal-site/public/skill-files/`. Skills catalog still 327. `npm run build` green — 327 `/skills/[slug]`, `/posts`, `/blog/[slug]` prerendered.

All other tracked submodules verified 0 commits behind upstream:
`DoWhiz(dev) ca38f810`, `gstack(main) 94993f74`, `marketingskills(main) 7868cb92`,
`mattpocock-skills(main) 84fdeffd`.

Worktree housekeeping: `current-projects/{DoWhiz,first-tree}` came up pointing
at pre-recorded commits older than what the branch already tracked. Aligned
with `git submodule update --init <path>` before checking upstream — same
class of drift as 2026-08-10.

### Social sweep — 0 new across X / XHS

**X** (`x.com/search?q=from:bingran_bry -filter:replies&f=live`, logged-in
Chrome MCP): `find` returned 6 status links; top was `2085906880380252502`
(Aug 7) — matches the latest tracked in `posts.json`. Chrome extension present
this cycle (previously absent) — deviceId `14fa2aac-...`, Browser 1, macOS.
Session budget: 1 nav + 1 find, well under §2.4 caps.

**XHS** (`xiaohongshu.com/user/profile/60b2d6d00000000001006eb7`, anonymous
Chrome — no login required for read): SPA rendered 10 `section.note-item`
entries this cycle (vs 28 yesterday, 10 on 2026-08-09). DOM still strips
`data-note-id` and empties `href` to `/explore/`, but each card carries the
visible title. Cross-referenced all 10 titles against `posts.json` — every one
matches a tracked entry (top: "让 Agent 救活 Agent" = `xhs-6a2b7072000000001700ba7f`
2026-06-12). No new XHS notes. Card count variance across cycles (10 / 28 / 10)
continues to be non-deterministic under anonymous access — matches the
2026-08-10 note; titles remain the reliable identity check.

**YouTube**: not re-fetched this cycle — RSS cadence unchanged.

### CI + shipping

- PR: https://github.com/bingran-you/bingran-you/pull/545 — `sweep 2026-08-12: submodules (2) + 0 posts`
- Checks: skills-sync-check pass (2m2s), Vercel `Canceled by Ignored Build Step` (expected — no `personal-site/` diff, `vercel.json` ignoreCommand skipped the build).
- Merged via `gh api -X PUT .../pulls/545/merge -f merge_method=squash` (local `gh pr merge --squash` refused because `main` is checked out at the sibling `/Users/bingranyou/Downloads/bingran-you` worktree — API path bypasses the local git op cleanly). Merge commit `faeac712`. Remote branch deleted via `gh api -X DELETE`.

### Recurring notes

- `gh pr merge --squash --delete-branch` fails from a worktree when `main` is checked out elsewhere ("fatal: 'main' is already checked out at ..."). Use `gh api -X PUT .../merge` + `gh api -X DELETE .../refs/heads/<branch>` instead. Consider folding this into a TOOLS.md note.
- Chrome MCP was connected this session (Browser 1, macOS) — first time in a scheduled sweep in a while. That means the "extension absent" baseline from 2026-08-10 is no longer permanent; treat connectivity as variable per session.
