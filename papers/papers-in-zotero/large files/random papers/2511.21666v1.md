# PDF Document: 2511.21666v1.pdf

**File Path:** 2511.21666v1.pdf

**Processed Date:** 2026-02-10T18:17:30.332Z

**File Size:** 10841.91 KB

**Total Pages:** unknown

**Extracted Pages:** unknown

**PDF Library:** Zotero PDFWorker

**Attachment Item ID:** 3803

**Collection:** Large Files > Random Papers

---

## Extracted Text Content

Uncertainty Quantification for Visual
Object Pose Estimation
Lorenzo Shaikewitz1, Charis Georgiou1, and Luca Carlone1
Abstract—Quantifying the uncertainty of an object’s pose estimate is essential for robust control and planning. Although pose estimation is a well-studied robotics problem, attaching statistically rigorous uncertainty is not well understood without strict distributional assumptions. We develop distribution-free pose uncertainty bounds about a given pose estimate in the monocular setting. Our pose uncertainty only requires high probability noise bounds on pixel detections of 2D semantic keypoints o-
n a known object. This noise model induces an implicit, non-convex set of pose uncertainty constraints. Our key contribution is SLUE (S-Lemma Uncertainty Estimation), a convex program to reduce this set to a single ellipsoidal uncertainty bound that is guaranteed to contain the true object pose with high probability. SLUE solves a relaxation of the minimum volume bounding ellipsoid problem inspired by the celebrated S-lemma. It requires no initial guess of the bound’s shape or size and is guarante-
ed to contain the true object pose with high probability. For tighter uncertainty bounds at the same confidence, we extend SLUE to a sum-of-squares relaxation hierarchy which is guaranteed to converge to the minimum volume ellipsoidal uncertainty bound for a given set of keypoint constraints. We show this pose uncertainty bound can easily be projected to independent translation and axis-angle orientation bounds. We evaluate SLUE on two pose estimation datasets and a realworld drone tracking scenar-
io. Compared to prior work, SLUE generates substantially smaller translation bounds and competitive orientation bounds. We release code at https://github.com/MIT-SPARK/PoseUncertaintySets.
I. INTRODUCTION
Vision-based object pose estimation is an important problem in robotics, used in planning and control for manipulation [1], [2] and navigation [3]. As with any point estimate, a rigorous notion of uncertainty is crucial for robust downstream decision-making. An autonomous vehicle must slow down to avoid a traffic cone with uncertain position, but can drive past one clearly on the sidewalk. Among the plethora of pose estimation
This work was supported by the AFOSR “Certifiable and SelfSupervised Category-Level Tracking” program, Carlone’s NSF CAREER award, and the ONR RAPID program. L. Shaikewitz is supported by an NSF graduate research fellowship. L. Carlone holds concurrent appointments at MIT and as an Amazon Scholar. This paper describes work performed at MIT and is not associated with Amazon. 1Authors are with the Laboratory for Information and Decision Systems, Massachusetts Institute of Technology, Cambridge, MA. -
Emails: {lorenzos, cgeo, lcarlone}@mit.edu.
Fig. 1: Conformal Pose and Uncertainty Estimation. Given an RGB image of an object (a), we extract 2D semantic keypoints and conformal uncertainty sets (b) which contain the ground truth keypoint with high probability. These sets imply a non-convex set of quadratic constraints on the object pose. We use a generalization of the S-Lemma and a projection scheme to reduce this set to an explicit bound (c) containing the true object pose with high probability and centered at the pose estimate, highligh-
ted in black.
techniques, there are many uncertainty heuristics. These include ensembles [4], Bayesian inference (e.g., particle filters [5]), and learned uncertainty models [6], [7], [8], [9]. Although these heuristics carry no statistical guarantees (or require strong assumptions to obtain guarantees), they generally capture uncertainty intrinsic to the problem. In monocular estimation, this includes scale ambiguity and object symmetries. Instead of a heuristic, we develop statistically rigorous pose uncertai-
nty which also captures scale ambiguity (although it does not capture object symmetry). Further, our uncertainty bounds hold under minimal distributional assumptions and are agnostic to the choice of pose estimator. We phrase the distribution-free uncertainty estimation problem as estimating an explicit uncertainty set which contains the true object pose with high probability. To
arXiv:2511.21666v1 [cs.RO] 26 Nov 2025

capture the scale ambiguity inherent to the monocular setting, we begin with high-probability uncertainty sets about a sparse set of RGB keypoint measurements (obtained with, e.g., conformal prediction) and propagate keypoint uncertainty to pose uncertainty. Following [10], the uncertainty estimation problem lies in bounding this non-convex pose constraint set with an interpretable outer bound. Prior work simplifies this problem by fixing the shape of the outer bound, for example to a spherical b-
ound [10], [11]. We remove this simplification and directly solve for minimum-volume ellipsoidal outer bounds on pose uncertainty. Contributions. We present S-Lemma Uncertainty Ellipsoid estimation (SLUE), a relaxation hierarchy to compute explicit and expressive pose uncertainty bounds. As the name suggests, SLUE relies on a generalization of the classical S-lemma [12] to optimize directly for the shape of an ellipsoidal bound on pose uncertainty. SLUE is guaranteed to give an outer bound on the -
pose constraints implied by keypoint uncertainty. The first-order relaxation gives a fast but conservative uncertainty bound, while the hierarchy trades computation for tighter bounds. We reduce the computational cost by using infinity-norm keypoint constraints and a quaternion formulation. The SLUE bound describes pose uncertainty in an interpretable manner which is easy to project to independent translation and axisangle orientation bounds. We demonstrate SLUE on two object-focused datasets [13]-
, [14] and a real-world drone tracking scenario [15] To summarize, our contributions are: • An efficient and statistically rigorous algorithm for pose uncertainty quantification using an ellipsoidal outer bound. • A relaxation hierarchy guaranteed to converge to the minimum volume ellipsoidal uncertainty bound at a given confidence. • A projection scheme to reduce joint pose uncertainty bounds to interpretable translational and angular uncertainty bounds. • Extensive experiments on three real-worl-
d datasets. The rest of the paper is organized as follows. We discuss related work and introduce notation in Sections II and III, respectively. We then describe our keypoint noise model and develop the uncertainty estimation problems in Section IV. We relax the minimum volume ellipsoid problem into a tractable uncertainty estimation problem using the generalized S-lemma in Section V, and generalize this approach to a hierarchy of relaxations guaranteed to converge to the minimum ellipsoid bound in-
 Section V-C. In Section VI we reduce ellipsoidal uncertainty into interpretable bounds on translation and orientation. Lastly, we evaluate our method on three realworld datasets in Section VII.
II. RELATED WORK
Object Pose Uncertainty. There are several popular heuristics for pose uncertainty quantification. Ensemble methods [4], [16] aggregate multiple pose estimates into an approximation of estimator variance. Such a strategy has no grounding in the quality of the estimator and thus no statistical guarantees. A more precise approach is to discretize the object into particles to track [5] or distinct regions [8] and model uncertainty locally. This and other continuous correspondence ambiguity approaches-
 [9], [17] are generally used to capture orientation uncertainty in the case of symmetric objects, but still lack guarantees. The most common way uncertainty estimation is integrated into pose estimation pipelines is through learned estimates, which are often used to select the best pose in a multi-hypothesis setting [18], [6], [19], [20], [7]. Among these works [7] is most similar to ours, using a neural network to propagate estimated keypoint uncertainty to a pose. All of these approaches, howev-
er, lack rigorous statistical guarantees. We draw on an emerging body of statistically rigorous distribution-free pose uncertainty quantification from conformal prediction [21]. In particular, Yang and Pavone [10] compute conformal bounds on keypoint error and use a semidefinite relaxation to maximize the pose error consistent with the keypoint bounds. Follow-up works improve uncertainty estimation with sample-based inner approximation [22] (thus sacrificing uncertainty guarantees) or simplify to -
a generalized Chebyshev center problem [11]. A key limitation of these approaches is expressiveness: [10], [11] fix the shape of the outer bound (i.e., consider spherical bounds) and optimize only for scale. This hides the effects of scale ambiguity and leads to conservative bounds. A simpler approach for fixed uncertainty bound shape is to apply conformal prediction directly to the pose estimate. In this paper, we solve a more expressive relaxation that captures scale ambiguities by optimizing di-
rectly for the shape of a statistically rigorous outer uncertainty bound. Minimal Bounding Sets. Reducing a compact set into a simple geometric object, such as a minimum volume bounding ellipsoid, has major computational and interpretability advantages [23]. When the set is convex, finding a bounding ellipsoid of minimal volume is known as the Löwner-John ellipsoid problem [24] and admits a solution using convex optimization. The nonconvex case is more difficult. Nie et al. [25] propose a sum-of-s-
quares relaxation hierarchy which always returns an ellipsoidal outer bound and converges to the minimum-trace bounding ellipsoid. The log determinant objective, which gives minimum volume, is dismissed as impractical. Other works [26], [11] rely on strong simplifying assumptions. In particular, [11], [10] fix the shape of the ellipsoid bound, trading expressiveness for

Fig. 2: Keypoint measurements. Given a 3D model with annotated 3D keypoints (left), we assume pixel detections of the location of each keypoint in the image frame (right). Pixel keypoint measurements also carry an uncertainty bound, shaded in blue.
certifiably optimal scale estimation. In this paper, we give a new perspective on the sum-of-squares approach based on the generalized S-Lemma. Under our approach, the log determinant objective is numerically stable and we directly solve for the minimum volume bound.
III. NOTATION
Symbols for vectors and matrices are bolded to distinguish them from scalars. When there are multiple subscripts, we wrap a vector or matrix in parentheses to indicate specific elements. For example, (εi)1 denotes the first element of the vector εi. For integer indexing, we use the shorthand [n] ≜ 1, ..., n. We write the ith 3D basis vector as eˆi for i ∈ [3]. We denote the Kronecker product with ⊗ and use the vec(·) operator to denote the vectorization of a matrix by stacking its columns. The set-
 Sn is all symmetric n × n matrices and the set Sn is any n-dimensional unit vector. Polynomials. For non-negative integer κ, we denote the κ-order monomial basis in variable x ∈ Rn by [x]κ ∈ RC(n+κ−1,κ), where C(·, ·) is shorthand for the binomial coefficient. For example, [x]2 is defined as:
[x]2 ≜ x21 x1x2 . . . x2n
T. (1)
Note that [x]1 = x and [x]0 = 1. We denote the set of polynomials in x of order at most κ by Rκ[x]. Let x1 = 1 so that [x]κ contains all monomials up to order κ. Then, any p ∈ R2κ[x] may be written in quadratic form as p(x) = [x]κTA[x]κ for some (possibly non-unique) constant matrix A. We use the notation p(x) ⪰sos 0 to mean p(x) may be represented by some matrix A ⪰ 0.
IV. PROBLEM FORMULATION: POSE UNCERTAINTY
ESTIMATION FROM BOUNDED KEYPOINT NOISE
We seek an estimate of object pose uncertainty from RGB pixel measurements of object keypoints and their corresponding keypoint uncertainty sets (see Figure 2). We describe this measurement uncertainty model in Section IV-A and propagate keypoint uncertainty to pose uncertainty constraints in Section IV-B. Distinct from [10], we consider infinity-norm keypoint uncertainty and explicitly propagate the coverage probability to the pose uncertainty constraint set.
A. Measurement Model: Keypoint Uncertainty Sets
Given an RGB image of a target object, we measure the pixel locations of a sparse set of 3D CAD-modelframe keypoints (Figure 2). For each 3D keypoint, we use a front-end (i.e., a neural network) to detect its noisy pixel location in the image frame [27]. We model measurement noise using keypoint uncertainty sets. That is, we assume the ground truth pixel keypoint is within a norm-ball about the detected pixel keypoint with high probability. In practice, we obtain these sets using conformal predict-
ion (Section VII-A). This gives the following bounded uncertainty model. Denote the fixed 3D model-frame keypoints by bi ∈ R3 for i ∈ [N ] and their corresponding pixel measurements by yi = [ui, vi, 1]T. For an object with position t ∈ R3 and orientation R ∈ SO(3) in the frame of a camera with intrinsics K, the keypoint measurement model reprojects the 3D points and adds arbitrary noise:
yi = K(Rbi + t)
ˆe3 · (Rbi + t) + εi, (2)
where εi ∈ R2 is some measurement noise in homogeneous form ((εi)3 ≡ 0). We make no distributional assumptions for εi. Instead, assume its p-norm is bounded with probability 1 − αi:
P(∥εi∥p ≤ ri(αi)) ≥ 1 − αi. (3)
In practice, we obtain the radii ri(αi) for a given confidence αi using split conformal prediction [28] (detailed in Section VII-A). In this paper we treat ri(αi) as a given constant and drop the αi dependence. In the following, we specialize to the case p = ∞, which corresponds to square axis-aligned uncertainty sets with side length 2ri (see Figure 3). As we will show, the ∞-norm has strong computational advantages over the 2-norm used in [10]; we give the p = 2 case in Appendix A for completene-
ss.
B. Uncertainty Constraint Set for 6D Pose
Given the measurement model (2) and noise bound (3) we now derive a pose uncertainty constraint set that contains the true pose with high probability. Combining

Image Plane
y1
y2
y3
y4
v
u
Backprojected 3D points
for each 2D keypoint
XY
Z
Camera
Fig. 3: Pose Uncertainty Constraint Set. The infinity-norm bounds on keypoint error (left) each imply a cone of backprojected 3D feasible keypoint positions (right). Combining the bounds for multiple keypoints and imposing object shape constraints yields an implicit pose uncertainty constraint set which contains many feasible poses.
eqs. (2, 3), the following reprojection constraint holds with probability at least 1 − αi:
yi − K(Rbi + t)
ˆe3 · (Rbi + t) ∞
= ∥εi∥∞ ≤ ri. (4)
It is also reasonable to assume detected keypoints are visible to the camera. Thus, the projection of bi must have positive depth. This is the chirality (frontof-camera) constraint for keypoint i:
ˆe3 · (Rbi + t) > 0. (FoC)
To proceed we convert the rational reprojection constraint (4) to a polynomial backprojection constraint. The infinity norm constrains each coordinate independently. Multiplying (4) by the depth and using chirality (FoC) to drop its absolute value, eq. (4) reduces to two inequality constraints which hold with probability at least 1 − αi:
(yiˆeT
3 − K)(Rbi + t) · ˆej ≤ riˆeT
3 (Rbi + t), (BP∞)
for j = 1, 2. Each absolute value yields two constraints linear in R and t; thus, (BP∞) is four linear inequalities. Pose Uncertainty Constraint Set. Combining the backprojection and chirality constraints for each keypoint gives the following pose uncertainty constraint set, visualized in Figure 3.
Proposition 1 (Pose Uncertainty Constraint Set). Assume measurements of N object keypoints of the form (2) and noise bounded in infinity-norm with high probability as (3). The true position tgt and orientation Rgt of the object are contained in the following constraint set:
t ∈ R3
R ∈ SO(3)
(FoC)i (BP∞)i
for i ∈ [N ] , (P∞)
with probability at least β.
For arbitrary dependence among εi, β ≥ 1−PN
i=1 αi.
In the extreme where εi is independent from εj for all i ̸= j then β = QN
i=1(1 − αi). When the εi are perfectly positively correlated and all αi ≡ α, we have β = 1−α.
Proof. For perfect correlation the probability of (BP∞)i for all i is the same as the probability for any choice of i. Under independence, the probability of the intersection of (BP∞)i for each i reduces to a product. For arbitrary dependence, the result follows from a union bound.
Contrary to [10], we observe the statistical guarantee of covering the ground truth pose holds with probability less than 1−α in general. The worst case confidence can quickly become uninformative. For example, if N = 10 and αi ≡ α = 0.1 for all i, the set (P∞) is guaranteed to contain the ground truth with probability greater than 0. In practice, however, we expect some positive correlation; thus, coverage should generally exceed the independence bound. For our choice of α and N , the independenc-
e bound gives a 35% coverage probability. We provide empirical coverage results in Section VII. The set (P∞) is an implicit representation of the set of poses which are consistent with the backprojection and chirality constraints (see Figure 3). We first seek a simpler characterization of this set, choosing to model uncertainty as an ellipsoidal bound centered on a single pose estimate (as in Figure 4). Our procedure is agnostic to the choice of pose estimator, and the estimate itself does not nee-
d to be inside (P∞). We use standard perspective-n-point [29] to generate a pose estimate (  ̄R,  ̄t) (see Appendix E). Given an estimated pose, we seek a single ellipsoid which bounds the pose uncertainty.

Problem 1 (Joint Ellipsoidal Bound). Reduce the pose constraints (P∞) into a single ellipsoidal uncertainty bound of minimal volume (Figure 4) which is centered at the pose estimate and contains the ground truth pose with probability at least β. That is, find H ≻ 0 such that (R, t) ∈ (P∞) implies:
vec(R −  ̄R) t − t ̄
T
H vec(R −  ̄R)
t − t ̄ ≤ 1. (5)
Problem 1 seeks a simple convex representation of the pose uncertainty constraints (P∞). We also desire explicit bounds on translation and axis-angle uncertainty.
Problem 2 (Rotation and Translation Bounds). Reduce the joint ellipsoidal bound (5) into translational and angular ellipsoids centered at the pose estimate (  ̄R,  ̄t) and containing the ground truth pose with probability at least β.
V. UNCERTAINTY ELLIPSOID VIA S-LEMMA
In this section we compute the ellipsoidal uncertainty bound (5) from Problem 1 (see Figure 4a). Our procedure, motivated by the S-lemma [12], offers a new and more intuitive perspective on the sum-of-squares approach for a minimum volume bounding ellipsoid [25]. We first write the pose constraints in quadratic form (Section V-A) and develop an S-lemma relaxation for the bounding ellipsoid problem in Section V-B. In Section V-C we extend this to a hierarchy of relaxations which is guaranteed to co-
nverge to the true minimum volume ellipsoid. Our approach is faster and more expressive than the outer approximations in [11], [10], optimizing directly for ellipsoid shape. We name the combined method SLUE, for S-Lemma Uncertainty Ellipsoid estimation.
A. Pose Uncertainty Set in Quadratic Form
First, we write (P∞) as a set of quadratic constraints in the homogenized variable x ≜ [1, rT, tT]T, where r ≜ vec(R) ∈ R9. To write the chirality and backprojection constraints in terms of r we use the Kronecker identity: MRbi = (bT
i ⊗ M)r for M = K or M = I3. This gives the following quadratic form of (P∞):
(
x ∈ R13 x1 = 1
xTAix ≤ 0, i ∈ [5N ] xTQjx = 0, j ∈ [15]
)
. (6)
In (6), the equality constraints enforce R ∈ SO(3) and the inequalities enforce the backprojection and chirality constraints. We give explicit Ai and Qj in Appendix B.
B. Bounding Ellipsoid via S-Lemma
Recalling Problem 1, we seek the minimum volume ellipsoid which outer-bounds the set (6) centered at the pose estimate (  ̄R, t ̄). Let  ̄r ≜ vec(  ̄R) and x ̄ ≜ [ ̄rT, t ̄T]T. In quadratic form, we seek H such that:
(6) =⇒ xT x ̄THx ̄ − 1 −x ̄TH
−Hx ̄ H
| {z }
≜W(H)
x ≤ 0. (7)
In words, any pose that satisfies (7) and hence is in (P∞) must also be in the ellipsoid (5). Recall that H ≻ 0 is an unknown matrix defining the size and shape of the ellipsoid. To find the minimum volume H satisfying (7), we turn to a generalization of the celebrated S-lemma [12].
Proposition 2 (Generalized S-Lemma). Let x ∈ Rn and W, Yi, Zj ∈ Sn be given matrices for i ∈ [N ] and j ∈ [M ]. For the following statements, (ii) =⇒ (i).
(i) (primal) xTYix ≤ 0 and xTZjx = 0 for i ∈ [N ] and j ∈ [M ] =⇒ xTWx ≤ 0.
(ii) (dual) There exist μ ∈ RM and λ ∈ RN , λi ≥ 0
∀ i such that W ⪯ PN
i=1 λiYi + PM
j=1 μj Zj .
Proof. Pre and post-multiplying (ii) by xT and x with non-negative λ, statement (i) holds by substitution.
The classical S-lemma states that the primal and dual are equivalent ((ii) ⇐⇒ (i)) when there is only one inequality constraint: N = 1 and M = 0. Proposition 2 (also called the lossy S-lemma [30]) extends this to multiple constraints at the cost of a possible duality gap. To apply Proposition 2 to our problem, notice that the primal (i) corresponds exactly to (7), while the dual (ii) provides a semidefinite constraint which may be incorporated into a convex optimization problem. Thus, we relax the-
 constraint (6) to its convex dual (ii) and optimize for the minimum volume ellipsoid satisfying (ii). The ellipsoid’s volume is inversely proportional to log det(H) [31], leading to the following result.
Proposition 3 (Bounding Ellipsoid). An outer bounding ellipsoid for the set (6) is given by the solution to the following convex optimization problem.
max
H∈S12, H⪰0 λ∈R5N , μ∈R15
log det(H)
s.t. W(H) ⪯
5N
X
i=1
λiAi +
15
X
j=1
μj Qj ,
λi ≥ 0, i ∈ [5N ].
(8)
The maximizer H⋆ is an ellipsoid centered at x ̄ as (5).

(a) κ = 1 (b) κ = 2 (c) κ = 3 (d) κ = 4
Fig. 4: Hierarchy of Bounding Ellipsoids. We solve for an ellipsoidal representation (5), in blue, of a set defined by several quadratic constraints (P∞), in purple, which may be non-convex. Our approach (SLUE) admits a hierarchy of ellipsoidal bounds guaranteed to converge to the minimum volume bound as relaxation order κ tends to infinity. In this 2D toy example, the relaxation converges by κ = 3.
The proof follows directly from Proposition 2. In applying Proposition 2, we implicitly dropped the homogenization constraint x1 = 1. Clearly, if statement (ii) holds for general x then it holds when x1 = 1. The S-lemma relaxation, however, means we only guarantee (8) returns some outer ellipsoidal bound (not necessarily the smallest) on the pose uncertainty constraint set. To improve this bound, the next section extends (8) into a relaxation hierarchy, depicted in Figure 4, which is guaranteed to-
 converge to the true minimum volume ellipsoidal bound.
C. The S-Lemma Relaxation Hierarchy
In the previous section we used the generalized Slemma to compute an ellipsoid which bounds the pose uncertainty set (P∞). Here, we use ideas from sum-ofsquares (SOS) programming [32] to derive an SOS Slemma and extend the ellipsoid procedure into a hierarchy of relaxations. This hierarchy, visualized in Figure 4, enjoys a guarantee of convergence to the true minimum volume ellipsoid bounding (P∞). Our algorithm is similar to [25]; our formulation is based on the S-lemma and solves directly for th-
e minimum ellipsoid volume. Further, our approach overcomes previously reported numerical difficulties [11], solving to optimality with offthe-shelf solvers such as MOSEK [33]. The key idea of the SOS S-lemma hierarchy is to use dual polynomials in the S-lemma relaxation. We introduce this by analogy to Proposition 2. Written as an SOS inequality, the linear matrix inequality in statement (ii) of Proposition 2 reads:
xTWx ⪯sos
N
X
i=1
λixTYix +
M
X
j=1
μjxTZjx. (9)
The SOS S-lemma simply generalizes the dual variables in (9) to polynomials λi(x) and μj(x).
Proposition 4 (SOS Generalized S-Lemma). Let x ∈ Rn and W, Yi, Zj ∈ Sn be given matrices for i ∈ [N ] and
j ∈ [M ]. Fix integer κ ≥ 0. For the following statements, (ii) =⇒ (i).
(i) (primal) xTYix ≤ 0 and xTZjx = 0 for i ∈ [N ] and j ∈ [M ] =⇒ xTWx ≤ 0.
(ii) (dual) There exist polynomials λi, μj ∈ R2κ[x] with λi(x) ⪰sos 0 for i ∈ [N ] and j ∈ [M ] such that:
xTWx ⪯sos
N
X
i=1
λi(x)xTYix +
M
X
j=1
μj (x)xT Zj x.
(10)
The quantity κ + 1 is called the relaxation order.
Proof. For polynomial p(x) ∈ R[x], p(x) ⪯sos 0 =⇒ p(x) ≤ 0. Thus, statement (i) holds by substitution.
Similar to Proposition 2, the SOS generalized Slemma provides a tractable relaxation of the polynomial constraint set (6). Although the sum-of-squares notation makes it more difficult to interpret, (10) is simply a linear matrix inequality among matrices in Sd, where d is the dimension of the monomial basis [x]κ. Appendix C gives an explicit example. The case κ = 0 corresponds exactly to (9), while larger κ give higher-order relaxations. Notice that each κ relaxation is a special case of the κ + 1-
 relaxation with the highest order term of the dual polynomials set to zero. Thus, increasing κ gives a more expressive relaxation. This provides an elegant way to trade off computation (the SOS inequality is a linear matrix inequality of size d × d) for relaxation accuracy.
As in the first-order case, we use Proposition 4 to derive a hierarchy of bounding ellipsoids.
Proposition 5 (Bounding Ellipsoid Hierarchy). For fixed relaxation order κ + 1, an outer bounding ellipsoid for the set (6) is given by the solution to the following convex

optimization problem.
max
H∈S12, H⪰0 λi, μj ∈R2κ[x], i∈[5N ], j∈[15]
log det(H)
s.t. xTW(H)x ⪯sos
5N
X
i=1
λi(x)xTAix
+
15
X
j=1
μj (x)xT Qj x,
λi ⪰sos 0, i ∈ [5N ].
(11)
The maximizer H⋆κ defines an ellipsoid in terms of x centered at x ̄ as in (5). Further, for any κ the κ+1 relaxation is an upper bound: log det(H⋆κ) ≤ log det(H⋆κ+1).
Proposition 5 simply applies the SOS generalized Slemma (Proposition 4) to relax the bounding ellipsoid problem. It can be efficiently expressed as a semidefinite program by converting the SOS inequality into a linear matrix inequality. We can also show the relaxation is asymptotically tight with κ.
Theorem 6 (Hierarchy Convergence [25], [11]). The maximizer H⋆κ of (11) gives the minimum volume ellipsoid bounding (6) as relaxation order κ tends to infinity.
We prove Therorem 6 in Appendix C, drawing from [25]. Although the theorem only guarantees asymptotic convergence, we observe finite convergence at low relaxation orders occurs in practice (see e.g., Figure 4) and finite convergence guarantees have been obtained in related problems [34]. In this paper we only solve (11) up to second-order (κ = 1). For speed, we prefer the quaternion representation of the set (P∞) which requires only 4 variables to represent rotations instead of 9 (see Appendix D).-
 We use TSSOS [35] to write the polynomial inequality (10) and modify the dual problem to match (11). When it is not clear from context, we use SLUE-κ to refer to SLUE with a κ-order relaxation.
VI. TRANSLATIONAL AND ANGULAR UNCERTAINTY
The ellipsoid (5) expresses rotation and translation uncertainty jointly. However, it is not immediate to infer rotation uncertainty from the ellipsoidal bound on joint translation and rotation matrix uncertainty. In view of Problem 2, we next describe a simple projection scheme to obtain separate bounding ellipsoids for translation and orientation, each interpretable as explicit uncertainty bounds.
Translational Ellipsoid. To reduce (5) into a translation-only ellipsoid, we project H onto its last three coordinates via orthogonal projection [36]. Let Pt ≜ 03×9 I3 . The translations t ∈ R3 satisfying (5) also satisfy the following ellipsoidal constraint:
(t −  ̄t)THt(t − t ̄) ≤ 1, (12)
where Ht ≜ PtH−1PtT
−1 is the ellipsoid matrix. Angular Ellipsoid. The angular ellipsoid requires more care. Similar to translations, we can project H onto its first nine coordinates. This gives the ellipsoid:
(r − r ̄)THr(r − r ̄) ≤ 1, (13)
where Hr ≜ PrH−1PrT
−1 and Pr ≜ I9 09×3 .
Eq. (13) is still an implicit rotation constraint because rotation matrices must satisfy the additional constraints R ∈ SO(3). Thus, we transform (13) into an ellipsoid over axis-angle deviation from the rotation estimate  ̄R. For an angle θ and axis ω ∈ S2, any rotation matrix R ∈ SO(3) can be written as an axis-angle perturbation of  ̄R. That is, R = Rω(θ)  ̄R, where Rω(θ) rotates by angle θ about axis ω according to Rodrigues’ rotation formula [37, Ch. 6.2]. Specifically, Rω(θ) = I3 + ωˆ sin(θ)-
 + ωˆ2(1 − cos(θ)), where ωˆ is the skew-symmetric cross-product matrix for ω:
ωˆ ≜


0 −ω3 ω2 ω3 0 −ω1 −ω2 ω1 0

 (14)
For convenience, we represent axis-angle using the vector ω sin(θ). Assuming θ ≤ 90◦, we can recover ω from its direction and θ from its norm. The vector ω sin(θ) is particularly convenient because it is related to the skew part of the rotation matrix. From Rodrigues’ formula, R − RT = 2 sin(θ)ωˆ. In vector form, this is:
2ω sin(θ) =


[Rω(θ)]3,2 − [Rω(θ)]2,3 [Rω(θ)]1,3 − [Rω(θ)]3,1 [Rω(θ)]2,1 − [Rω(θ)]1,2

. (15)
Using (15), we can project the rotation matrix ellipsoid to an ellipsoid in ω sin(θ). As an intermediate step, we rewrite (13) as an ellipsoid in Rω(θ). Notice that r− ̄r =
vec (Rω(θ) − I3)  ̄R . By the Kronecker identity,
r − r ̄ = (  ̄RT ⊗ I3)vec(Rω(θ) − I3). (16)
The vector vec(Rω(θ) − I3) can then be projected to ω sin(θ) using (15) (subtracting the identity leaves the skew part unchanged). This procedure is summarized in the proposition below.
Proposition 7. Let Pθ be the projection matrix such that Pθvec(Rω(θ)) = (15). Define the matrix Hθ as below:
Hθ ≜ 4 Pθ (R ̄ T ⊗ I3)THr(  ̄RT ⊗ I3) −1 PT
θ
−1
. (17) If θ ≤ 90◦, the set of axis-angle deviations (ω, θ) from the rotation estimate  ̄R satisfying (5) also satisfy the following ellipsoidal constraint.
(ω sin(θ))THθ(ω sin(θ)) ≤ 1. (18)
At a given ξ in (18), ω = ξ/∥ξ∥2 and sin(θ) = ∥ξ∥2.

Restricting the angular deviation (θ ≤ 90◦) is not strictly necessary. Using the quaternion rotation representation gives a similar marginalization scheme which does not require any orientation restrictions, see Appendix D.
VII. EXPERIMENTS
In this section, we evaluate SLUE in three objectfocused scenarios. We first describe the three datasets. Then, in Section VII-A, we show how to obtain the high-probability keypoint uncertainty bounds (3) from split conformal prediction [28] and provide quantitative coverage results in Section VII-B. Our main results are in Section VII-C. In particular, we compute angular and translational bounds for each dataset. Compared to prior work, using SLUE to optimize directly for the bounding ellipsoid’s-
 shape yields significantly smaller uncertainty sets without sacrificing outer bound guarantees. In Section VII-D we show SLUE is the fastest bounding approach despite solving also for ellipsoid shape. Further qualitative results and ablations are in Appendix E. Datasets. We test SLUE on the LineMODOcclusion (LM-O) dataset [13], the YCB-Video (YCB-V) dataset [14], and the drone tracking scenario from [15] (CAST). LM-O and YCB-V are object-focused datasets, with RGB images of 8 and 21 tabletop obje-
cts, respectively. For LM-O we omit the eggbox object following [10]. For YCB-V, we test on the BOP subset of images [38] and include objects 1 to 15 (except for object 13, the bowl). We omit object 13 due to symmetry and objects 16 through 21 because there are few examples in the BOP subset. For a robotics setting, the drone tracking scenario CAST contains RGB images of a remote-controlled racecar taken from an aerial quadcopter. We use the entire image sequence which includes several laps of the-
 racecar. See Figure 5 for example images.
A. Keypoint Bounds via Conformal Prediction
Keypoint Detection. Given an RGB image, we use a learned front-end to extract the pixel positions of target object keypoints. In the LM-O and YCB-V datasets, we use the heatmap-based convolutional neural network from [39] for keypoint detection. This network was trained on the BOP synthetic image split [40]. Like [10], we take the pixel with highest confidence in each heatmap as detection. For CAST, we use the authorprovided keypoints, which come from a ResNet [41] detector trained on real-world i-
mages [15]. Conformal Prediction. We obtain keypoint uncertainty bounds, the input to SLUE through eq. (3), using split conformal prediction [21], [28]. Conformal prediction provides high-probability error bounds through a
calibration process. During calibration, we test keypoint detection on a subset of images (separate from training or testing data) where the ground truth keypoint positions are known (e.g., from human annotation). Under the assumption of exchangeability (a slight relaxation of independent and identically distributed, see [28]), performance on the calibration data predicts performance on test data. We now describe our calibration routine. For each keypoint yi we compute uncertainty bounds ri(α) at -
fixed confidence α. For LM-O we calibrate on the BOP image subset (following [10]), which contains 200 images selected from the full dataset. For CAST, we calibrate on a randomly selected subset of 200 images in the video sequence. In both datasets, we remove the calibration images from our evaluation set to ensure exchangeability with the test images. In YCB-V, we instead calibrate on 2000 synthetic images of the objects freshly generated with BlenderProc [42]. Calibrating on synthetic images may-
 violate exchangeability (the calibration images are no longer randomly drawn from the test data), but is more realistic for a real-time system. Given an RGB image, let yi denote the detected pixel location of keypoint i and let ci ∈ [0, 1] be its associated confidence (for CAST, the network confidence is always 1). Using (2), the ground truth pixel coordinate is zi ≜ K(Rgtbi + tgt)/(ˆe3 · (Rgtbi + tgt)). We measure performance during conformal calibration using the following confidence-weighted d-
istance metric:
s((yi, ci), zi) = ci∥yi − zi∥p, (19)
where p = 2 or p = ∞. The keypoint uncertainty bound ri(α) (visualized in Figure 3) is given by an adjusted 1 − α quantile of calibration distance scores. Specifically, let r ̃i(α) be the adjusted (1 − α)(1 + 1/n) quantile of the scores of keypoint i on all the calibration images, where n is the number of calibration images picturing keypoint i. Then, ri(α) ≜ r ̃i(α)/ci. By [28, Thm. 3.2], the highprobability error bound (3) holds when calibration and evaluation data are exchangeable.
B. Empirical Coverage Results.
To evaluate our high-probability uncertainty guarantees, we compute keypoint and pose coverage. Coverage is defined as the percentage of keypoints (or poses) with uncertainty sets that contain the ground truth keypoint (or pose). In Table I we provide empirical coverage of ground truth keypoints in each keypoint uncertainty set at two selected confidences: α = 0.1 and α = 0.4. We also give coverage of the ground truth pose in both the pose uncertainty constraint set (P∞) and the firstorder boundin-
g ellipsoid from SLUE (5) (more details

(a) CAST position uncertainty (b) LM-O uncertainty (c) YCB-V uncertainty
Fig. 5: Image-plane projections of ellipsoidal pose uncertainty. Plots show the set of possible poses in the second-order joint ellipsoidal bound for α = 0.1. Uncertainty is mostly concentrated along the optical axis. For CAST, we only show translation uncertainty about the pose estimate.
α = 0.1 α = 0.4
Kpts (P∞) SLUE Kpts (P∞) SLUE
CAST 90.9 56.5 86.5 58.3 5.6 19.5 LM-O 90.7 68.5 91.1 60.2 15.1 45.8 YCB-V 93.0 72.9 90.9 47.4 5.4 20.2
TABLE I: Mean Empirical Coverage (%). Keypoints (kpts) have a tight 1 − α coverage guarantee from conformal prediction. The pose uncertainty constraint set (P∞) has a worst case guarantee of 1 − N α coverage, but we observe it falls between independence (1 − α)N and perfect positive correlation 1 − α. For these datasets, N is between 7 and 11. SLUE is only guaranteed to be more conservative than (P∞).
in Section VII-C). The former roughly quantifies the probability β in Proposition 1. We caution that coverage results are subject to several confounding factors including the inaccuracy of hand-labeled ground truth poses given by the datasets. We report the mean coverage for each dataset in Table I. For the datasets with real-world calibration (LMO and CAST), the empirical coverage nearly matches the 1 − α guarantee. For YCB-V, which uses synthetic calibration, the α = 0.1 bounds slightly over cov-
er while the α = 0.4 bounds under cover. We caution that LMO and YCB-V ground truth poses are imprecise [10]. Small errors in pose are magnified when propagated to keypoints. For pose coverage, Table I shows the pose uncertainty constraint set suffers a 30 − 45% drop in coverage compared to keypoints. There are 8 − 11 keypoints per object for LM-O, and 7 per object for CAST. This renders the worst-case bound 1 − N α (Proposition 1) near useless. The pose coverage is slightly better than the indepe-
ndence case, suggesting our bounds benefit
from some positive correlation between the keypoints. Lastly, the ellipsoidal uncertainty set at order 1 (SLUE) is not overly conservative. For α = 0.1, it achieves around 90% coverage (this is a coincidence, we can only guarantee the ellipsoid is more conservative than (P∞)). The ellipsoid is a little tighter for α = 0.4. We provide quantitative and qualitative uncertainty bound results in the next section.
C. Pose Uncertainty Bounds
In this section, we compute ellipsoidal pose uncertainty bounds for the set (P∞) using SLUE. In particular, we compute first and second-order bounding ellipsoids (Section V) using the pose estimates from PnP as center. After solving for these ellipsoids we marginalize to ellipsoids in translation and rotation space via projection as outlined in Section VI. We show quantitative and qualitative results for α = 0.1 and leave qualitative results for α = 0.4 to Appendix E. Baselines. We compare SLUE ag-
ainst RANSAG [10] and GRCC [11]. Both methods solve relaxations of the bounding ellipsoid problem where the ellipsoid shape is fixed and optimize only for its scale, solving the rotation and translation problems separately. They also use 2-norm keypoint uncertainty, which does not affect coverage guarantees. Following [11], we use a spherical bound and solve for its scale. We test RANSAG with a first and second-order relaxation and GRCC with only a second-order relaxation (GRCC requires at least s-
econd-order). We use a custom Julia implementation of RANSAG and rely on the open-source MATLAB code for GRCC. Results exclude image frames where one or more methods reports failure, leaving 6070/6190 images for LM-O, 3153/3256 images for YCB-V, and 1587/1691 images for CAST. Quantitative Results. Figure 6 shows the cumulative distribution function (CDF) of the volume of the transla

(a) Translation bound CDF for CAST. (b) Translation bound CDF for LM-O. (c) Translation bound CDF for YCB-V.
(d) Rotation bound CDF for CAST. (e) Rotation bound CDF for LM-O. (f) Rotation bound CDF for YCB-V.
Fig. 6: Volume of Translational and Angular Uncertainty Ellipsoids. We plot the cumulative distribution function (CDF) of each approach at first and second-order. Our translation and angular bounds are significantly tighter at first-order, while only the translation bounds are conclusively tighter for a second-order relaxation.
tion and rotation ellipsoids for each method in Figure 6. The translation volume is (4π/3) log det(Ht). For the orientation ellipsoid, the volume is (4π/3)θ1θ2θ3, where θi is the ith principal axis of the ellipsoid Hθ, in degrees, clipped above 90◦ to be consistent with our assumptions. Each method computes an outer approximation of pose uncertainty at a fixed confidence, so a smaller volume generally indicates a better approximation.
As Figure 6 shows, our approach (SLUE) excels at producing tight translation bounds. In particular, the first and second-order S-lemma ellipsoids are smaller than their baselines by multiple orders of magnitude. Interestingly, SLUE-1 is often much smaller than even the second-order ellipsoid generated by RANSAG or GRCC. The key reason for this improvement is optimizing directly for the shape of the uncertainty ellipsoid.
In the angular case, SLUE significantly outperforms RANSAG for a first-order relaxation but its advantages are less clear at second-order. A confounding variable is the choice of keypoint uncertainty set norm. RANSAG and GRCC use p = 2, while SLUE uses p = ∞ for its computational benefits. Although the uncertainty set confidences are the same, the p = ∞ case may allow more extreme angular changes. Another difference is that RANSAG and GRCC optimize for an independent rotation bound instead of solv-
ing a single problem for
a joint ellipsoid. Nevertheless, SLUE-2 is competitive across the datasets. A key strength of our approach is that it only requires solving a single optimization problem to obtain an angular and translational bounding ellipsoid. We quantify this computational advantage in Section VII-D.
Qualitative Results. To complement the CDF plots, we provide qualitative plots of the ellipsoids generated by SLUE and RANSAG. Figure 7 shows translational and angular uncertainty ellipsoids at first and secondorder. The ellipsoids are plotted for duck object in frame 342 of the LM-O dataset, but they are representative examples. Figure 5 shows the image-plane projection of the second-order ellipsoid bounds at this frame, along with representative frames for the other two datasets.
Figure 7a captures the phenomenon of increased translation uncertainty along the optical axis of the camera. The first-order ellipsoid is clearly very conservative along the optical axis (it expands to include the origin), but remains relatively tight along orthogonal axes. The second-order ellipsoid (Figure 7b) is small enough to serve as a useful translation bound, and clearly contains all the sampled points. Both SLUE ellipsoids are far more expressive than the RANSAG ellipsoids and accurately -
capture the uncertainty due to scale ambiguity. In contrast, the qualitative rotation ellipsoids (Figure 7c)

(a) Translation uncertainty ellipsoids. (b) Close view of second-order translation uncertainty ellipsoids.
(c) Angular uncertainty ellipsoids.
Fig. 7: Qualitative bounding sets in rotation and translation space. Projections of the first and second-order uncertainty ellipsoids generated by SLUE and RANSAG [10] for the duck object on LM-O (α = 0.1). SLUE optimizes directly for the shape of the ellipsoid, leading to expressive uncertainty sets. The ellipsoids enclose all samples from (P∞).
do not clearly show an elongated axis from the monocular setup. In this example, the first-order RANSAG orientation bound is greater than 90 degrees, forming the unit sphere in our axis-angle coordinates. While the second-order SLUE ellipsoid is smaller, the first-order SLUE ellipsoid is slightly tighter along the z axis. In general, the SLUE angular bounds are quite conservative relative to the samples and second-order RANSAG. This is an artifact of increased rotation uncertainty from using ∞-nor-
m keypoint sets and the difficultly of bounding quadratic equality constraints with an ellipsoid. For a more interpretable perspective, Figure 5 shows image-plane projections of poses on the surface of the joint second-order uncertainty ellipsoid. We show frames 568 and 1000 for CAST, frame 342 for LM-O, and frame 501125 for YCB-V. Due to reprojection, the uncertainty bounds are dominated by the pose with translation closest to the camera. The uncertainty bounds are relatively tight and a much ric-
her description of pose than a single estimate. The image-plane bounds benefit from the joint uncertainty representation, which maintains correlation between translation and orientation uncertainty. For CAST we only plot translation uncertainty (no 3D model is available). The translation bounds are relatively small. We show additional results and ablations in Appendix E.
D. Runtime Breakdown
We conclude our experiments with a comparison of runtimes on LM-O. In Table II, the keypoint detector is implemented in PyTorch and runs on an M3 Mac. Our method (S-Lemma Bounds) and RANSAG are implemented in Julia, while GRCC uses the released MATLAB implementation. All methods are running on
Relaxation order κ = 1 (ms) κ = 2 (ms)
Keypoint detection 48.8 48.8 SLUE 39.0 548 RANSAG [10] 238 3,671 GRCC [11] - 36,531*
TABLE II: Comparison of Mean Runtimes on LM-O
a single CPU thread. For our method we omit the translation and rotation marginalization (which are simple linear algebra operations and have marginal effect on runtime). RANSAG and GRCC compute translation and rotation bounds separately; Table II adds the two times. The GRCC runtime is inflated due to its MATLAB implementation; we indicate this with an asterisk. Runtimes are reported for confidence α = 0.1. Our method is significantly faster than prior work, running faster than even keypoint dete-
ction at first-order. We benefit from a simpler infinity-norm pose uncertainty set, solving only a single optimization problem, and from the more flexible ellipsoid volume objective. The computational benefits are even more extreme at second-order, where the quaternion implementation (Appendix D) of SLUE reduces the number of decision variables.
VIII. CONCLUSION
We presented SLUE: a hierarchy of algorithms to generate ellipsoidal object pose uncertainty bounds which are statistically guaranteed to cover the true object pose with high probability. SLUE may be used to add statistically rigorous uncertainty to any pose estimator. It relies only on the assumption of keypoint noise which is bounded with high-probability. While the set of object

pose constraints is non-convex, we use a generalization of the S-lemma to formulate a convex relaxation for the minimum volume ellipsoid bounding the pose uncertainty set. This relaxation admits a sum-of-squares hierarchy guaranteed to converge to the true minimum volume bounding ellipsoid. From the joint ellipsoid bounds we use a simple projection scheme to bound translation and axis-angle error separately. Real-world evaluations show our method achieves tighter translation bounds and competitiv-
e angular bounds while being significantly faster than prior work.
Limitations and Future Work. While our work provides a rigorous and practical strategy for obtaining tight pose uncertainty bounds from keypoint error bounds, we do not make use of the high-probability nature of the keypoint bounds. Future work should consider that some keypoints may be outside of their uncertainty sets, which leads to a combinatorial multiple-testing problem. This is already well-understood in the maximum likelihood literature as outlier-robust estimation and could be applied to -
obtain better pose estimates and uncertainty. For some computational penalty, our S-lemma relaxation could be modified to estimate the ellipsoid center jointly with its shape [25]. It would also be useful to capture orientation uncertainty due to, for example, object symmetry. This would require a more expressive primitive or keypoint conformal score. From a more practical perspective, robotics applications often see sequential frames picturing the same object; in the tracking setting, the fast fi-
rst-order uncertainty could be fused or incorporated into an active perception scheme.
REFERENCES
[1] B. Wen, C. Mitash, B. Ren, and K. E. Bekris, se(3)-tracknet: Datadriven 6d pose tracking by calibrating image residuals in synthetic domains, IEEE/RSJ Intl. Conf. on Intelligent Robots and Systems (IROS), (2020), 10367–10373. [2] A. H. Li, P. Culbertson, V. Kurtz, and A. D. Ames, DROP: Dexterous Reorientation via Online Planning, preprint, (2025). arXiv:2409.14562.
[3] Y. Peng, Deep learning for 3D Object Detection and Tracking in Autonomous Driving: A Brief Survey, preprint, (2023)..
[4] G. Shi, Y. Zhu, J. Tremblay, S. Birchfield, F. Ramos, A. Anandkumar, and Y. Zhu, Fast Uncertainty Quantification for Deep Object Pose Estimation, preprint, (2021). arXiv:2011.07748. [5] X. Deng, A. Mousavian, Y. Xiang, F. Xia, T. Bretl, and D. Fox, PoseRBPF: A Rao-Blackwellized Particle Filter for 6D Object Pose Tracking, Robotics: Science and Systems (RSS), (2019). [6] E. Brachmann, F. Michel, A. Krull, M. Y. Yang, S. Gumhold, and c. Rother, Uncertainty-Driven 6D Pose Estimation of Objects an-
d Scenes From a Single RGB Image, Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), (2016). [7] Y. Lin, J. Tremblay, S. Tyree, P. A. Vela, and S. Birchfield, Keypoint-Based Category-Level Object Pose Tracking from an RGB Sequence with Uncertainty Estimation, 2022 International Conference on Robotics and Automation (ICRA), (2022), 1258–1264. [8] T. Hodanˇ, D. Baráth, and J. Matas, EPOS: Estimating 6D Pose of Objects with Symmetries, IEEE Conference on Computer Vi-
sion and Pattern Recognition (CVPR) (2020).
[9] B. Okorn, M. Xu, M. Hebert, and D. Held, Learning Orientation Distributions for Object Pose Estimation, 2020 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS), (2020), 10580–10587.
[10] H. Yang and M. Pavone, Object Pose Estimation with Statistical Guarantees: Conformal Keypoint Detection and Geometric Uncertainty Propagation, preprint, (2023)..
[11] Y. Tang, J-B. Lasserre, and H. Yang, Uncertainty quantification of set-membership estimation in control and perception: Revisiting the minimum enclosing ellipsoid, Proceedings of the 6th Annual Learning for Dynamics and Control Conference, 242, Proceedings of Machine Learning Research, PMLR, (2024), 286–298. [12] I. Pólik and T. Terlaky, A Survey of the S-Lemma, SIAM Review, 49(3) (2007) 371–418. [13] E. Brachmann, A. Krull, F. Michel, S. Gumhold, J. Shotton, and C. Rother, Learning 6d object-
 pose estimation using 3d object coordinates, IEEE Conf. on Computer Vision and Pattern Recognition (CVPR), (2014), 536–551. [14] Y. Xiang, T. Schmidt, V. Narayanan, and D. Fox, PoseCNN: A convolutional neural network for 6D object pose estimation in cluttered scenes, Robotics: Science and Systems (RSS), (2018). [15] L. Shaikewitz, S. Ubellacker, and L. Carlone, A Certifiable Algorithm for Simultaneous Shape Estimation and Object Tracking, IEEE Robotics and Automation Letters (RA-L) (2024). [16] K-
. Wursthorn, M. Hillemann, and M. Ulrich, Uncertainty quantification with deep ensembles for 6d object pose estimation, arXiv preprint arXiv:2403.07741 (2024). [17] R. L. Haugaard and A. G. Buch, Surfemb: Dense and continuous correspondence distributions for object pose estimation with learnt surface embeddings, Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition, (2022), 6749–6758. [18] J. Liu, W. Sun, H. Yang, Z. Zeng, C. Liu, J. Zheng, X. Liu, H. Rahmani, N. Sebe, -
and A. Mian, Deep learning-based object pose estimation: A comprehensive survey, arXiv preprint arXiv:2405.07801 (2024). [19] M. Tian, L. Pan, M. H. Ang, and G. H. Lee, Robust 6d object pose estimation by learning rgb-d features, IEEE Intl. Conf. on Robotics and Automation (ICRA), (2020), 6218–6224. [20] B. Wen, W. Yang, J. Kautz, and S. Birchfield, Foundationpose: Unified 6d pose estimation and tracking of novel objects, Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recogn-
ition, (2024), 17868–17879.
[21] G. Shafer and V. Vovk, A Tutorial on Conformal Prediction, J. of Machine Learning Research (2008) 51. [22] Y. Gao, Y. Tang, H. Qi, and H. Yang, CLOSURE: Fast Quantification of Pose Uncertainty Sets, Robotics: Science and Systems (RSS), (2024).
[23] J. B. Lasserre, A generalization of Löwner-John’s ellipsoid theorem, Mathematical Programming, 152(1) (2015) 559–591. [24] A. Ben-Tal and A. Nemirovski, Lectures on Modern Convex Optimization: Analysis, Algorithms, and Engineering Applications, (2001) Society for Industrial.
[25] J. Nie and J. W. Demmel, Minimum Ellipsoid Bounds for Solutions of Polynomial Systems via Sum of Squares, Journal of Global Optimization, 33(4) (2005) 511–525. [26] M. Casini, A. Garulli, and A. Vicino, Feasible Parameter Set Approximation for Linear Models with Bounded Uncertain Regressors, IEEE Transactions on Automatic Control, 59(11) (2014) 29102920. [27] K. He, G. Gkioxari, P. Dollár, and R. Girshick, Mask R-CNN, Intl. Conf. on Computer Vision (ICCV), (2017), 2980–2988. [28] A. N. Angelo-
poulos, R. F. Barber, and S. Bates, Theoretical Foundations of Conformal Prediction, preprint, (2024). arXiv:2411.11824. [29] G. Terzakis and M. Lourakis, A consistently fast and globally optimal solution to the perspective-n-point problem, European Conf. on Computer Vision (ECCV), (2020), 478–494. [30] L. Lessard, The S-Lemma. https://laurentlessard.com/teaching/ me7247/supplementary/S-lemma.pdf. [31] S. Boyd and L. Vandenberghe, Convex optimization, (2004) Cambridge University Press.

[32] J. B. Lasserre, Global optimization with polynomials and the problem of moments, SIAM J. Optim., 11(3) (2001) 796–817.
[33] MOSEK ApS, The MOSEK Optimizer API for Julia. Version 11.0.30 (2025). https://docs.mosek.com/latest/juliaapi/index.html. [34] J. Nie, Optimality conditions and finite convergence of Lasserre’s hierarchy, Mathematical programming, 146(1-2) (2014) 97–121. [35] J. Wang, V. Magron, J. B. Lasserre, and N. H. A. Mai, CSTSSOS: Correlative and term sparsity for large-scale polynomial optimization, arXiv preprint arXiv:2005.02828 (2020). [36] W. Karl, G. Verghese, and A. S. Willsky, Reconstructing ell-
ipsoids from projections.
[37] T. Barfoot, State Estimation for Robotics, (2017) Cambridge University Press. [38] T. Hodanˇ, M. Sundermeyer, B. Drost, Y. Labbé, E. Brachmann, F. Michel, C. Rother, and J. Matas, BOP Challenge 2020 on 6D Object Localization, European Conference on Computer Vision Workshops (ECCVW) (2020). [39] K. Schmeckpeper, P. Osteen, Y. Wang, G. Pavlakos, K. Chaney, W. Jordan, X. Zhou, K. Derpanis, and K. Daniilidis, Semantic keypoint-based pose estimation from single RGB frames, arXiv preprint arXiv:220-
4.05864 (2022). [40] T. Hodanˇ, F. Michel, E. Brachmann, W. Kehl, A. G. Buch, D. Kraft, B. Drost, J. Vidal, S. Ihrke, X. Zabulis, C. Sahin, F. Manhardt, F. Tombari, T-K. Kim, J. Matas, and C. Rother, BOP: Benchmark for 6D Object Pose Estimation, European Conf. on Computer Vision (ECCV), (2018), 19–35. [41] K. He, X. Zhang, S. Ren, and J. Sun, Deep residual learning for image recognition (2016) 770–778.
[42] M. Denninger, D. Winkelbauer, M. Sundermeyer, W. Boerdijk, M. Knauer, K. H. Strobl, M. Humt, and R. Triebel, BlenderProc2: A Procedural Pipeline for Photorealistic Rendering, Journal of Open Source Software, 8(82) (2023) 4901.
[43] H. Yang and L. Carlone, A Quaternion-based Certifiably Optimal Solution to the Wahba Problem with Outliers, Intl. Conf. on Computer Vision (ICCV), (2019).
APPENDIX A
2-NORM POSE UNCERTAINTY CONSTRAINT SET
For completeness and alignment with [10], we give the pose uncertainty constraint set when measurement noise is bounded in 2-norm with high probability. As before, the measurement noise bounds give the following reprojection constraint:
yi − K(Rbi + t)
ˆe3 · (Rbi + t) 2
= ∥εi∥2 ≤ ri. (A20)
The chirality constraints are the same as (FoC). For the backprojection constraints, square the inequality (A20) and multiply through by the depth. The following inequality holds with probability at least 1 − αi:
(yiˆeT
3 − K)(Rbi + t) 2
2 ≤ (riˆeT
3 (Rbi + t))2. (BP2)
The pose uncertainty constraints are simply the set of (BP2) and (FoC) for each keypoint. Note that there is only one backprojection inequality for each keypoint, but it is quadratic in t and R. The 2-norm uncertainty may be used in place of ∞-norm uncertainty for the remainder of SLUE.
APPENDIX B
EXPLICIT FORMS FOR CONSTRAINT MATRICES
This section gives explicit forms for the constraint matrices Qk and Ai used in Section V. We write the constraint matrices in sparse form, specifying only the nonzero elements. The notation (i, j, l) means Qi,j = Qj,i = l. Let R ∈ SO(3) and denote its columns by ri, i ∈ [3]. Recall that x ≜ [1, rTtT]T. We first write the equality constraint matrices Qk, which enforce R ∈ SO(3). Three matrices enforce constrain the columns of R to unit 2-norm (∥ri∥2 = 1):
Q1 : (2, 2, 1), (3, 3, 1), (4, 4, 1), (1, 1, −1), Q2 : (5, 2, 1), (6, 3, 1), (7, 4, 1), (1, 1, −1), Q3 : (8, 2, 1), (9, 3, 1), (10, 4, 1), (1, 1, −1).
(A21)
Three matrices enforce orthogonal columns:
r1 · r2 = 0 ⇒ Q4 : (2, 5, 1), (3, 6, 1), (4, 7, 1), r1 · r3 = 0 ⇒ Q5 : (2, 8, 1), (3, 9, 1), (4, 10, 1), r3 · r2 = 0 ⇒ Q6 : (5, 8, 1), (6, 9, 1), (7, 10, 1).
(A22) The final 9 equality matrices enforce the right hand rule cross products between the columns of R, elementwise. The constraint r1 × r2 = r3 gives:
Q7 : (3, 7, 1), (4, 6, −1), (1, 8, −1), Q8 : (4, 5, 1), (2, 7, −1), (1, 9, −1), Q9 : (2, 6, 1), (3, 5, −1), (1, 10, −1).
(A23)
The constraint r2 × r3 = r1 gives:
Q10 : (6, 10, 1), (7, 9, −1), (1, 2, −1), Q11 : (7, 8, 1), (5, 10, −1), (1, 3, −1), Q12 : (5, 9, 1), (6, 8, −1), (1, 4, −1).
(A24)
Lastly, r3 × r1 = r2 gives:
Q13 : (9, 4, 1), (10, 3, −1), (1, 5, −1), Q14 : (10, 2, 1), (8, 4, −1), (1, 6, −1), Q15 : (8, 3, 1), (9, 2, −1), (1, 7, −1).
(A25)
In (6), the inequality constraint matrices Ai correspond to the backprojection and chirality constraints. The first N matrices capture chirality. Using the Kronecker identity, the chirality constraint is:
(FoC)i ⇐⇒ ˆeT
3 ((bT
i ⊗ I3)r + t) > 0. (A26)
Noting all terms are linear, (A26) is:
(FoC)i ⇐⇒ ˆe3T(bT
i ⊗ I3) ˆe3T
r
t > 0. (A27)
This expression gives the first row and column of Ai, which correspond to multiplying r and t by 1. That is,
Ai = −


0 ˆe3T(bT
i ⊗ I3) ˆe3T (bi ⊗ I3)ˆe3 0 0 ˆe3 0 0

. (A28)

The backprojection constraints are also rewritten using the Kronecker identity. For each keypoint i, let:
d±
ij ≜ ri(bT
i ⊗ I3)Tˆe3 ± (KT − ˆe3yT
i )(bi ⊗ I3)ˆej
riˆe3 ± (KT − ˆe3yT
i )ˆej
,
(A29) for j = 1, 2. There are four backprojection constraints (± for j = 1, 2). In linear form,
(BP∞)i ⇐⇒ d±
ij
Tr
t ≤ 0. (A30)
These are 4N constraints and may be written in matrix form as (A28) to give the remaining Ai matrices.
APPENDIX C
SUM-OF-SQUARES S-LEMMA
A. Example SOS Inequality
The SOS inequality is a convenient shorthand for a linear matrix inequality (LMI) arising from polynomials. To illustrate, we rewrite (10) for a second-order relaxation (κ = 1) and x = [1, x2]T. Assume without loss of generality that there are no equality constraints (M = 0) and let λi(x) = xTAx for A ⪰ 0. Eq. (10) is:
xTWx ⪯sos
N
X
i=1
xTAixxTYix. (A31)
The right hand side is a fourth-order polynomial. To convert to an LMI we write each term in quadratic form with [x]2 ≜ [1, x2, x22]T ∈ R3. Consider i = 1 and let:
A1 ≜ a b
b c . (A32)
The polynomial xTA1x is the sum of 3 terms:
xTAix = a + 2bx2 + cx2
2 (A33)
Thus, the product xTA1xxTY1x can be written as
[x]2TYˆ 1[x]2, where Yˆ 1 is:


aY1
0 0 000

+


0
0 bY1
000

+


000
bY1
0 0

+


000
0
0 cY1

.
(A34) Similarly, xTWx can be written as [x]2TWˆ [x]2 where:
Wˆ ≜ W 0
0 0 . (A35)
Repeating for all i, the matrix inequality form of (A31) is:
Wˆ ⪯
N
X
i=1
Yˆ i, (A36)
subject to Ai ⪰ 0 for all i. There are automatic tools including TSSOS [35] which perform this calculation.
B. Proof of Therorem 6: Hierarchy Convergence
Let p⋆ denote the optimal volume of the true minimum volume ellipsoid bounding (6). Let d⋆κ be the optimal ellipsoid volume from the (κ + 1)-order dual given in eq. (11). Proposition 4 guarantees d⋆κ ≤ p⋆ for all κ.
Our proof will perturb p⋆ slightly and show it lower bounds d⋆κ for sufficiently large κ. We use the following positivestellensatz which holds whenever the set (6) is compact.
Theorem A8 ([25]). Define a compact set P ≜ {x ∈ Rn : hi(x) ≤ 0, i ∈ [N ]} with polynomials hi ∈ R[x]. Any polynomial p(x) < 0 for x ∈ P satisfies:
p(x) ⪯sos
N
X
i=1
λi(x)hi(x), (A37)
where each λi(x) is an SOS polynomial.
Therorem A8 is a powerful result connecting sum-ofsquares polynomials with strictly positive polynomials. By replacing each equality with two inequalities, (6) is composed of 5N + 30 polynomial inequality constraints. Thus, we can apply the theorem to any polynomial strictly negative on (6). We’ll choose a perturbation of the optimal ellipsoid. Let gH(x) ≤ 0 be the ellipsoid (7) defined by H. Let the primal optimal ellipsoid matrix be H⋆. Perturb H⋆ to a slightly larger ellipsoid by H ̃ ≜ H⋆ − εI1-
2 with
ε > 0. We have log det(H ̃ ) = p⋆−o(ε). The perturbation implies g  ̃H(x) < 0 on the set (6). By Therorem A8:
g  ̃H(x) ⪯sos
N
X
i=1
λi(x)hi(x), (A38)
for some SOS polynomials λi(x), i ∈ [5N + 30]. This is exactly the constraint enforced by the dual problem, with the distinction that λi(x) may be a poly
nomial of any order. Thus, the ellipsoid H ̃ is feasible for (11) if κ is large enough. In practice we must restrict the polynomial order for a computationally tractable problem. However, in the limit of unbounded κ, the following holds for all ε > 0:
p⋆ − o(ε) ≤ κli→m∞ d⋆
κ ≤ p⋆. (A39)
Thus, the dual hierarchy converges to the primal.
APPENDIX D
FASTER UNCERTAINTY BOUNDS VIA QUATERNIONS
We derive the backprojection and chirality constraints for the quaternion representation of (P∞). This representation reduces the number of variables, significantly improving computation for higher-order relaxations.

A. Quaternion Arithmetic
The unit quaternion representation of a rotation about axis ω ∈ S2 by angle θ is:
q = cos(θ/2)
ω sin(θ/2) , (A40)
where q1 is the scalar part by convention. Unit quaternions have double coverage of the rotation space (q and −q represent the same rotation). Negating just the vector part gives q−1. Applying a rotation to a vector requires quaternion algebra. To rotate a point y ∈ R3:
q◦ 0
y ◦ q−1 = 0
Ry , (A41)
where ◦ denotes the quaternion product and R ∈ SO(3) is the rotation matrix corresponding to the quaternion q. It also composes rotations: q1 ◦ q2 = R1R2. Quaternion products can be written as matrix-vector products. Given a ∈ R4 and b ∈ R4,
a ◦ b = Ω1(a)b = Ω2(b)a. (A42)
which defines the following product matrices [43]:
Ω1(a) ≜

  
a1 −a2 −a3 −a4 a2 a1 −a4 a3 a3 a4 a1 −a2 a4 −a3 a2 a1

  
, Ω2(a) ≜

  
a1 −a2 −a3 −a4 a2 a1 a4 −a3 a3 −a4 a1 a2 a4 a3 −a2 a1

  
.
(A43) When we apply Ω1 or Ω2 to vectors c ∈ R3 we implicitly homogenize with a leading 0, making the matrices skew symmetric. These matrices allow converting between the matrix and quaternion representations of rotation with only vector products.
Lemma A9. Let x, y ∈ R3 and let q ∈ S3 and R ∈ SO(3) be the quaternion and matrix representations of the rotation (ω, θ). Then,
xTRy = −qTΩ1(x)Ω2(y)q. (A44)
Proof. From (A41) and (A42),
0
Ry = q ◦ Ω2(q−1) 0
y = Ω1(q)Ω2(q)T 0
y,
(A45) where we use the identity Ω2(q−1) = Ω2(q)T. Taking the inner product with x, we arrive at the identity:
xTRy = Ω2(q) 0
x
T
Ω1(q) 0
y
= qTΩ1(x)TΩ2(y)q. (A46)
The property Ω1(q)Ω2(q)T = Ω2(q)TΩ1(q) may be checked by substitution. Applying the skew symmetric property yields the lemma.
B. Quaternion Uncertainty Bounds
Notice that the chirality constraints (FoC) and backprojection constraints (BP∞) are linear in the rotation matrix R. For computational efficiency, we can rewrite these constraints in terms of the corresponding unit quaternion q. The chirality constraint is:
(FoC) ⇐⇒ −qTΩ1(ˆe3)Ω2(bi)q + eˆT
3 t ≥ 0, (A47)
and the backprojection constraints are:
(BP∞) ⇐⇒ qTΩ1 riˆe3 ± (K − yiˆeT
3 )Tˆej Ω2(bi)q
≤ riˆe3 ± (K − yiˆeT
3 )Tˆej
Tt. (A48)
Note there are 4 backprojection constraints for each keypoint i (± for j = 1, 2). To enforce that q represents a rigid rotation we simply require it have unit 2-norm: qTq = 1. Additionally, to handle double coverage we add the additional constraint qTq ̄ > 0 to force the quaternion into the same hemisphere as the rotation estimate q ̄. We now write the quadratic form of (P∞) in terms of unit quaternions. Let x ≜ [1, q, t]. In quadratic form, the pose uncertainty set is
(
x ∈ R8 x1 = 1
xTQx = 0
xTAix ≤ 0, i ∈ [5N + 1]
)
. (A49)
In the above, Q enforces the unit quaternion constraint, while the inequality matrices Ai enforce the chirality, backprojection, and double coverage constraints. Bounding this set with an ellipse is identical to Section V. Crucially, the quaternion representation enables substantial computational speedup by reasoning over a smaller 8×8 lifted matrix instead of a 13×13 matrix. In practice, the first-order relaxation fails to solve but the second-order relaxation is much tighter than the firstorder -
in matrix form and solves in about 500 ms.
C. Angular Ellipse Marginalization
The joint quaternion-translation ellipse matrix H ∈ S8 implies a translation and axis-angle ellipse similar to Section VI. Marginalizing to a translation-only ellipse requires projection onto the last three coordinates as before. The quaternion representation allows a slightly different axis-angle representation which eliminates the need for a bounded angular deviation. First, we project H to its first four coordinates to obtain an ellipse in q. Let Pq ≜ I4 04×3 and Hq ≜ PqH−1PqT
−1. Then the joint ellipse implies:
(q − q ̄)THq(q − q ̄) ≤ 1. (A50)
Let qθ perturb q by angle θ about axis ω ∈ S2. We can rewrite q = qθ ◦ q ̄. Thus,
q−q ̄ = qθ − 1
0 ◦q ̄ = cos(θ/2) − 1
ω sin(θ/2) ◦q ̄. (A51)

We choose ω sin(θ/2) as our axis-angle representation. Note that we no longer require θ ≤ 90◦ to uniquely recover the axis and angle from this representation. From (A51) and the identity (A42),
Hθ ≜ Pθ Ω2(q ̄)THqΩ2(q ̄) −1 PT
θ
−1
, (A52)
where Pθ simply projects onto the last 3 coordinates. The joint ellipse implies the following ellipsoidal constraint, completing the marginalization.
(ω sin(θ/2))THθ(ω sin(θ/2)) ≤ 1. (A53)
APPENDIX E
ADDITIONAL EXPERIMENTAL RESULTS
We provide additional results regarding pose estimation (Appendix E-A), ellipsoid bounds at confidence α = 0.4 (Appendix E-B), and several ablations of SLUE (Appendix E-C).
A. Pose Estimation
In our experiments, we choose perspective-n-point (PnP) [29] for pose estimation, which gives the center of ellipsoidal bound. Notably, our PnP formulation uses a Gaussian noise assumption informed by the keypoint bounds. We do not find a significant difference between this estimate and random sample averaging (RANSAG) [10], a heuristic which averages perspective-3-point solutions sampled from the keypoint bounds. We first present our PnP formulation and then explicitly compare the performance of -
the two estimators. The effectiveness of PnP highlights the flexibility of uncertainty quantification. SLUE generates accurate uncertainty sets regardless of the estimator priors. PnP Formulation. We solve the backprojection form of PnP using a second-order semidefinite relaxation. Assuming εi ∼ N (0, σiI2) in (2), the maximum likelihood estimator for object pose is:
min
R∈SO(3), t∈R3
N
X
i=1
1
σi
∥ˆe3 · (Rbi + t)yi − K(Rbi + t)∥2.
(A54) Note that the rotation constraints R ∈ SO(3) may be written as 15 quadratic equalities as in Appendix B. We simply set the variance to the keypoint bound: ri = σi. We solve (A54) using a second-order semidefinite relaxation [32]. This relaxation is empirically tight for all examples, meaning it always returns the maximum likelihood pose estimate.
Comparison with RANSAG. We compare Gaussian PnP against RANSAG [10]. For a fair comparison we use the 2-norm bounds for both methods. Table III computes
α = 0.1 α = 0.4
PnP RANSAG PnP RANSAG
LM-O 72.50 72.53 72.32 72.20 YCB-V 51.75 45.12 53.81 50.77 CAST 17.27 22.65 58.43 54.94
TABLE III: Pose 2D Projection Error (%)
the five-pixel 2D projection error metric [13] for each dataset. Neither method clearly outperforms the other, and both suffer when keypoint bounds are conservative, which is particularly acute in CAST.
B. Qualitative Bounds at α = 0.4
We provide additional qualitative ellipsoid bounds generated with confidence α = 0.4, which corresponds to smaller keypoint uncertainty bounds. These smaller bounds generally yield smaller uncertainty sets (which also hold with lower confidence). SLUE in turn produces smaller uncertainty ellipsoids, as shown in the ellipsoids in Figure 9 and image-plane projections in Figure 8. Both figures show the same image frames as Section VII-C. The figures still show similar ellipsoid trends. In particular,-
 the first-order translation ellipsoid is elongated along the optical axis while the second-order ellipsoid is significantly tighter.
C. Separate Ellipsoid Bounds and Relaxation Order
Lastly, we provide two ablations to justify our design choices. Specifically, we compare bound volumes when solving for rotation and translation ellipsoids separately, and we compare with results for a third-order relaxation. Both of these design changes improves individual bounds but come at a cost of substantially increased runtime. Separate Rotation and Translation Ellipsoids. We first compare SLUE with optimizing for rotational and translational ellipsoid volumes separately. Clearly, this shou-
ld always produce tighter independent rotational and translational bounds compared with the joint ellipsoid objective. We find optimizing for separate ellipsoids is computationally expensive at higher relaxation orders and offers only a small improvement on bounds. Further, it sacrifices the flexibility of starting with a joint ellipsoid since translation and orientation uncertainty are often correlated. In (8), the rotation-only ellipsoid objective corresponds to setting H as follows:
H ≜ Hr 0
0 03×3
, (A55)

(a) LM-O uncertainty (b) YCB-V uncertainty
Fig. 8: Image-plane projections of ellipsoidal pose uncertainty. Plots show the set of possible poses in the second-order joint ellipsoidal bound for α = 0.4, which are significantly smaller than α = 0.1. We omit CAST.
(a) Translation uncertainty ellipsoid (α = 0.4). (b) Orientation uncertainty ellipsoid (α = 0.4).
Fig. 9: Qualitative bounding sets in rotation and translation space. Projections of the first and second-order SLUE uncertainty ellipses for the duck object on LM-O. The α = 0.4 case gives a smaller uncertainty set at the cost of lower confidence. We omit baselines for visual clarity.
where Hr ∈ S9 defines the rotation-only ellipsoid. We must also replace the objective with log det(Hr); otherwise, it is always 0. Similarly, the translation-only ellipsoid objective corresponds to defining H as:
H ≜ 09×9 0
0 Ht
, (A56)
where Ht ∈ S3 and we again replace the objective with log det Ht, but redefine H as above elsewhere. Table IV compares the bound volumes using SLUE (joint), which optimizes for a joint ellipsoid and then projects to rotation and translation ellipsoids, against separately optimizing for rotation and translation ellip
soids directly (split). We also show the total runtime of each approach. With one exception (likely a numerical issue), the split approach produces smaller volume bounds. At first-order, the split approach is also faster. For second-order bounds, however, the split approach is significantly slower because it must solve two large SDPs. In both cases the split approach only offers a minor volume improvement. Compute-limited applications which only require independent bounds should consider using the-
 split formulation.
Relaxation Order. Although we have only shown first and second-order results, the relaxation hierarchy extends beyond this and is only guaranteed to converge to the minimum volume bound as relaxation order κ → ∞.

Trans. (m3 × 10−3) Ang. (deg3 × 105) Time (ms)
Joint Split Joint Split Joint Split
κ = 1 2.7 1.4 2.35 2.40 37 31 κ = 2 0.29 0.16 4.90 3.51 519 1056
TABLE IV: Ablation: Median Bound Volumes and Runtimes for Split and Joint Ellipsoid Bounds on LM-O
Trans. (m3 × 10−3) Ang. (deg3 × 105) Time (ms)
κ= 1 2 3 1 2 3 1 2 3
CAST 154 9.7 6.1 4.3 2.2 1.4 35 284 7044 LM-O 2.7 0.29 0.19 2.3 0.49 0.37 37.4 520 7697 YCB-V 5.8 0.70 0.41 4.2 1.6 1.0 42.9 363 7145
TABLE V: Median Bound Volumes and Runtimes Across Relaxation Orders
Implicit in the hierarchy is a computational trade-off between accuracy, which improves at higher order, and runtime, which deteriorates. Table V gives bound volumes and runtimes up to a third-order relaxation. Across datasets, the third-order relaxation takes a median time of about 7 seconds, compared to under half a second for the second-order relaxation. The bound volumes do not improve proportionally. Although there is still some improvement in volume between second and third-order, it is sign-
ificantly smaller than the improvement from first to second-order. In most applications, the precision of a second-order bound will likely be enough.

---

## Processing Information

- **Processing Library:** Zotero PDFWorker
- **Processing Date:** 2026-02-10T18:17:30.332Z
- **Text Length:** 72753 characters
- **Success:** Text extraction completed
- **PDF Library Used:** Zotero PDFWorker
- **Pages Processed:** unknown of unknown
