# 2026-08-09

## Scheduled sweep — 2 submodule bumps + 0 posts

### Submodule bumps

| Submodule | Old | New | Change |
| --- | --- | --- | --- |
| `current-projects/DoWhiz` (dev) | `537d419d` | `ca38f810` | 4 commits (PR #1882 evidence-acquisition guardrail, PR #1884 auditor-direction mode) |
| `trusted-external-repos/gstack` (main) | `960c3a8d` | `94993f74` | 1 commit — v1.61.0.0 "fix wave: guards failing open / silent failures" (9 fixes, 4 community PRs absorbed) |

All other tracked submodules verified 0 commits behind upstream. Skills
catalog holds at 327 (no additions/removals from source repos). `make
sync` clean; the gstack bump touches ~40 mirrored SKILL.md files under
`personal-site/public/skill-files/` plus `skills.generated.json`.

Workspace hygiene note: on entering the worktree, three submodules
(`trusted-external-repos/{gstack,marketingskills,mattpocock-skills}`)
had empty working trees while the recorded commits were intact —
`git status` reported them as "modified content" (files deleted).
Restored with `git submodule update --force <path>`; the recorded
commits matched what previous session bumped to. Root cause unknown
(possibly aggressive worktree cleanup somewhere), harmless once
restored.

### Social sweep — 0 new across X / XHS / YouTube

**X**: profile page (anonymous, built-in Browser MCP — `claude-in-chrome`
extension still not connected) surfaced 5 unique `bingran_bry` status
IDs. All already tracked. Latest still
`x-2085906880380252502` from 2026-08-08.

**XHS**: `xiaohongshu.com/user/profile/60b2d6d00000000001006eb7`
returned 10 `<section class="note-item">` entries as before — but the
anonymous DOM now strips `data-note-id` from the sections and empties
the anchor `href` to just `/explore/`. Prior session (2026-08-08) was
still able to enumerate 10 note IDs from these anchors; that no longer
works anonymously. Section count of 10 matches the last known baseline
(31 tracked, latest `xhs-6a2b7072000000001700ba7f` 2026-06-12), so
count-parity is the only proxy for "no burst happened." First-class
enumeration now requires the logged-in `claude-in-chrome` fallback —
same detection path § 3.3 of `social-scraping-policy` already
documents. No skill edit yet: doesn't change the recipe, just
tightens what the anonymous baseline can do.

**YouTube**: RSS
(`youtube.com/feeds/videos.xml?channel_id=UCcRHAbp8hxdW_6c_DCb5saw`)
returned 7 entries, all present in `posts.json`. Latest still
`youtube-ua0ojr8im1o` (2026-05-23).

### CI + site

`npm run build` green, 327 `/skills/[slug]` prerendered, `/posts`
static. PR incoming.
