# 2026-08-08

## Scheduled sweep — 3 submodule bumps + 1 X post

### Submodule bumps

| Submodule | Old | New | Change |
| --- | --- | --- | --- |
| `current-projects/DoWhiz` | `ef5691a7` | `537d419d` | 6 commits on `dev` |
| `current-projects/first-tree` | `eae2b2e9` | `5f88e37d` | 2 commits on `main` |
| `trusted-external-repos/gstack` | `a3259400` | `960c3a8d` | 1 commit on `main` (v1.60.2.0 free-suite drift fix) |

All other tracked submodules verified 0 commits behind upstream. Skills
catalog holds at 327 (no additions/removals from source repos). `make
sync` clean, no drift in `.agents/skills`, `.claude/skills`, or
`personal-site/lib/skills.generated.json`.

Note: `git submodule update --init` left `gstack` on a stale detached
HEAD (`a3259400` on a `garrytan/*` branch) even though the parent
index recorded the same commit. Reset to `main`, then bumped forward
one commit to `960c3a8d`.

### Social sweep — X: +1 post, XHS/YouTube: 0

**X**: profile page (built-in Browser MCP, anonymous — the paid
`claude-in-chrome` extension isn't connected this session, same as
yesterday) surfaced 5 unique status IDs. 4 already tracked. New:
`2085906880380252502` from 2026-08-08 — "damn did not realize 🧐"
with photo. Added via `add-post.mjs`, then reverted and hand-inserted
into `posts.json` to avoid the resort-on-add diff bloat (script
produced +270/-262, hand-insert is +8/-0).

Login-required paths hit the same walls as 2026-08-06:
- Search `x.com/search?q=from%3Abingran_bry` → redirects to `/i/jf/onboarding/web`.
- Syndication `srv/timeline-profile/screen-name/bingran_bry` → 0 entries anonymous.
- `nitter.net` returns HTTP 200 but body length 0.

So the X sweep is bounded by "what the unauthenticated profile page
shows to a fresh viewer" — pinned tweet + ~4 most recent. Fine for a
daily sweep; would miss anything back-dated or older than the top 4 if
Bingran posts a burst.

**XHS**: `xiaohongshu.com/user/profile/60b2d6d00000000001006eb7` still
gates note interactions (`登录即可查看 Ta 的笔记`), but the note
*titles* are still enumerable in the DOM (10 visible). Compared them
against `posts.json` — all 10 already tracked. Latest still
`xhs-6a2b7072000000001700ba7f` (2026-06-12).

**YouTube**: RSS
(`youtube.com/feeds/videos.xml?channel_id=UCcRHAbp8hxdW_6c_DCb5saw`)
returns 7 entries, all present in `posts.json`. Latest still
`youtube-ua0ojr8im1o` (2026-05-23).

### CI + site

`npm run build` green, 327 `/skills/[slug]` prerendered, `/posts`
static. PR incoming.
