# 2026-06-01

## Added mattpocock/skills to the workspace skill pipeline

- Added `https://github.com/mattpocock/skills.git` as the `trusted-external-repos/mattpocock-skills` submodule on branch `main` at `aaf2453fbdfe7a15c07f11d861224f34ab4b53cb`.
- Wired the repo into `scripts/sync_skills.sh` by adding each category directory under `trusted-external-repos/mattpocock-skills/skills/` as a source root and adding the submodule to `MANAGED_SUBMODULES`.
- Added a personal-site source rule so `/skills` entries from this repo show collection `mattpocock-skills`, author `Matt Pocock`, and GitHub URLs under `mattpocock/skills`.
- Ran `make sync`; mirrored skill count is now 289. Matt Pocock contributes 27 active mirrored/downloadable entries.
- Existing first-source-wins conflict behavior shadows two Matt entries: deprecated `qa` is shadowed by `trusted-external-repos/gstack/qa`, and in-progress `review` is shadowed by `trusted-external-repos/gstack/review`.
