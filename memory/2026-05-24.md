# 2026-05-24

## Thermo-nuclear code quality skill

- Vendored Cursor's `thermo-nuclear-code-quality-review` skill into `repo-skills/thermo-nuclear-code-quality-review/`, with MIT license text and upstream provenance metadata.
- Ran `scripts/sync_skills.sh init` because the managed skill source submodules were missing in this worktree, then ran `make sync`.
- `make sync` now mirrors the skill into `.agents/skills/` and `.claude/skills/`, and regenerates `personal-site/lib/skills.generated.json` plus `personal-site/public/skill-files/thermo-nuclear-code-quality-review.md`.
- Updated `AGENTS.md` so future code modifications must read and follow both `karpathy-guidelines` and `thermo-nuclear-code-quality-review`; `CLAUDE.md` remains a symlink to `AGENTS.md`.
