# 2026-05-04

- Extended `scripts/sync_skills.sh` so workspace skill discovery now also includes `trusted-external-repos/gstack/`, `trusted-external-repos/gstack/browser-skills/`, and `trusted-external-repos/gstack/openclaw/skills/`.
- Added support for mirroring source roots that contain their own `SKILL.md`, so the top-level `trusted-external-repos/gstack/SKILL.md` is now linkable as the `gstack` skill.
- Updated `AGENTS.md`, `TOOLS.md`, and `MEMORY.md` so future sessions know `gstack` is part of the mirrored workspace skill sources.
