# 2026-06-17

## Scheduled workspace + posts sweep

### Submodules + skills ([PR #381](https://github.com/bingran-you/bingran-you/pull/381), merged)
- `marketingskills` 3bea48e → 8bfcdff (v2.4.2 → v2.5.1, 7 commits — v2.5.0 cross-refs + v2.5.1 release).
- `open-design` 98477c0 → c782aeb (43 commits — CI topology + Playwright stability + Discord invite unification).
- `make sync` mirrored a new `offers` skill from marketingskills and refreshed `copywriting` / `launch` / `pricing` / `sales-enablement` SKILL.md cross-refs. Skill count 308 → 308 net (one add + four touched).
- Same `npm install` snag inside the worktree as 2026-06-13; install once, re-run `make sync`.

### gbrain fork sync — deferred
- `bingran-you/gbrain` is 3 commits behind `garrytan/gbrain master`. The historical fast-forward (`gh api repos/bingran-you/gbrain/merge-upstream -X POST -f branch=master`) was **denied this run by the auto-mode classifier** (reason: "pushes directly to fork master, bypassing PR review").
- Workaround for future scheduled runs: either (a) ask Bingran to allow `merge-upstream` for fork-only fast-forwards via a `Bash` permission rule, or (b) open a PR in `bingran-you/gbrain` that pulls upstream and squash-merges it. Option (a) is cleaner — the fast-forward is by design not a behavior change.

### Posts sweep ([PR #382](https://github.com/bingran-you/bingran-you/pull/382), merged)
- **X**: 5 new tweets since Jun 13 baseline, all dated 2026-06-16 (PT):
  - `2067038014941843491`, `2067009256809750789`, `2066952259762638995`, `2066874897784074360`, `2066872649872019503`.
  - Added via `npm run post:add -- <url> --date 2026-06-16` (id-only entries; react-tweet renders the rest).
- **XHS**: 0 new notes. Top 5 on Bingran.ai profile (`6a2b7072`, `6a284cbd`, `6a245451`, `6a1cbc74`, `6a1bdb30`) all already in `posts.json`.
- Date convention confirmed: previous entries (`2065262547041681704`/Jun 11 PT vs Jun 12 UTC, `2064907453641138431`/Jun 10 PT vs Jun 11 UTC) line up with PT — used PT for all 5 new entries.

### Workflow snags (carry-over from 2026-06-13)
- `gh pr merge --squash --delete-branch` still fails on this worktree ("'main' is already checked out at ~/Downloads/bingran-you"). Workaround: omit `--delete-branch`, then `git push origin --delete <branch>` after merge. Applied for both PRs today.
- `npm install` once in `personal-site/` inside fresh worktree before `make sync` (gray-matter import).

### Browser map (re-confirmed)
- Browser 2 (`14fa2aac-c4d5-4d3f-8031-41744dd02990`) → X-logged-in (`@bingran_bry`); not XHS-logged-in.
- Laptop (`20636fdd-9b8e-460c-822b-d50274eb93ab`) → XHS-logged-in (`Bingran.ai` / `60b2d6d00000000001006eb7`).

### XHS profile data shortcut
- `window.__INITIAL_STATE__.user.notes` returns the chronological list of note IDs without needing to scrape DOM tiles. Used today to confirm "no new XHS posts" in one JS call after one navigate + End-scroll.
- This is faster than the `find` "links to /explore/" path (which now returns 0 because tile click-handlers don't expose `<a href>`). Worth folding into [social-scraping-policy](.claude/skills/social-scraping-policy/SKILL.md) §3.6 as the canonical XHS profile-monitor recipe.
