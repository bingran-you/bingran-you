# 2026-08-06

## Scheduled sweep — 3 submodule bumps, skills catalog 332 → 327

PR [#521](https://github.com/bingran-you/bingran-you/pull/521), merged.

### Submodule bumps

| Submodule | Old | New | Change |
| --- | --- | --- | --- |
| `current-projects/first-tree` | `2d2e7ec2` | `333c01aba` | ~89 commits on `main` |
| `trusted-external-repos/open-design` | `e6aeb38da` | `66004e0cd` | ~32 commits on `main`, thru v0.18.0 |
| `trusted-external-repos/mattpocock-skills` | `2ffb184ff` | `8b36d4fb` | v1.2.2 |

All other tracked submodules verified 0 commits behind upstream.

### mattpocock upstream restructuring

Commit `c66bdee` upstream ("chore: remove six unused skills and the
personal bucket") deleted `skills/personal/`. Updated
`scripts/sync_skills.sh` to drop that source-root entry — first `make
sync` after the bump errored on the missing directory. Net catalog
delta: 332 → 327 skills.

- Removed: `design-an-interface`, `edit-article`, `obsidian-vault`,
  `request-refactor-plan`, `ubiquitous-language`.
- Added: `wait-what`.

### Social sweep — deferred (no logged-in Chrome MCP)

Attempted the daily X + XHS check per `social-scraping-policy`. In this
scheduled run the `claude-in-chrome` extension isn't connected, and the
built-in Browser MCP hits login walls on both platforms:

- **X**: `x.com/search?q=from%3Abingran_bry ...` redirects to
  `/i/jf/onboarding/web?redirect_after_login=...`. Syndication endpoint
  `srv/timeline-profile/screen-name/bingran_bry` returns `entries: []`
  for anonymous callers now (2026-08-06 — this used to serve tweets).
- **XHS**: `xiaohongshu.com/user/profile/60b2d6d00000000001006eb7`
  renders the login wall — notes area shows "登录即可查看 Ta 的笔记",
  every `/explore/…` href in DOM is gutted to just `/explore/`.
  (Prior 2026-08-03 sweep noted "public profile renders fine" without
   login; that regressed.)

Per policy, did not attempt to log in / create observer account. Last
known heads unchanged:
- X latest: `2080481076179562922` (2026-07-24)
- XHS latest: `xhs-6a2b7072000000001700ba7f` (2026-06-12)

Flagged in PR description. Bingran can rerun the social sweep from a
session where `claude-in-chrome` is connected to his real Chrome
profile.

### CI + site

`skills-sync-check` green. Vercel preview green (327 `/skills/[slug]`
prerender). Squash-merged PR #521; deleted branch on remote (local
`gh pr merge --delete-branch` tripped on the co-checked-out `main` in
`~/Downloads/bingran-you` — used `git push origin --delete` instead).
