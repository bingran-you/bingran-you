# 2026-06-02

## Scheduled workspace + posts sweep ([PR #317](https://github.com/bingran-you/bingran-you/pull/317), merged)

### Submodule + skills bumps
- `trusted-external-repos/open-design` 938721ab → 861c9cf6 (29 commits — landing hero refresh, Hallmark community skill, BYOK provider model cache key fix, MMS redesign workflow, Composio custom-auth docs, etc.).
- `current-projects/first-tree` 1f76bf97 → 5b6e813f (uninitialized in this worktree; bumped via `git update-index --cacheinfo` without `submodule update --init`, matching TOOLS.md hygiene).
- `make sync` regenerated mirrors: 289 → **302** skills. 13 new open-design entries: `brandkit`, `brutalist-skill`, `gpt-tasteskill`, `image-to-code-skill`, `imagegen-frontend-{mobile,web}`, `minimalist-skill`, `output-skill`, `redesign-skill`, `reference-design-contract`, `soft-skill`, `stitch-skill`, `taste-skill-v1`.
- `taste-skill` SKILL.md refreshed from upstream.
- Verified every other submodule (`gstack`, `marketingskills`, `mattpocock-skills`, `gbrain`, `claude-skills`, `skills`, `mews`, `DoWhiz`, `skillsbench`, `multiplexed-ion-photon`, `bingran-you-private`) matches its upstream HEAD via `gh api repos/.../commits/<branch>` — no other bumps needed.

### /posts adds
- **X originals (id-only, react-tweet renders the rest):**
  - `x-2061530851104674223` (2026-06-01) — "knowing a thing exists is much more important than getting that thing done"
  - `x-2061220426932789727` (2026-05-31) — "how will this plot look like after another year?"
- **XHS originals — clears the 2026-05-31 gap from [memory/2026-05-31.md](2026-05-31.md):**
  - `xhs-6a1cbc74000000003700f844` (2026-05-31) — "再过一年之后会变成什么样子？"
  - `xhs-6a1bdb30000000003701e3b9` (2026-05-30) — "震惊，Thariq 竟然用不完100刀/月的Claude"

### Workflow notes for future-me
- XHS profile-tile `<a>` hrefs **do not carry `xsec_token` when read via DOM/JS** in the new logged-in flow. The token only appears in `location.href` after the SPA navigates to the note (i.e. after clicking the tile, not after pure JS extraction). Workaround: `find` → `left_click` the tile → read `tabs_context_mcp` for the resulting `xsec_token`-bearing URL.
- The modal click leaves the OLD note's images in the DOM, so the second note's cover extraction can be polluted. Reliable fix: open each note in a **fresh tab** (`tabs_create_mcp`) once you have its `xsec_token` URL. Then `imgs` are clean per-note and the video-note's `background-image` selector returns the right poster.
- Video XHS notes (e.g. 震惊Thariq): cover is set as a CSS `background-image` on a wrapper, not an `<img>`. Query `Array.from(document.querySelectorAll('*')).filter(e => getComputedStyle(e).backgroundImage.includes('xhscdn'))` to recover it.
- `gh pr merge --squash --delete-branch` again hit the "main is already checked out at ~/Downloads/bingran-you" snag (recurring from 2026-05-31 memory); GitHub squash-merged anyway, then `git push origin --delete <branch>` cleaned the remote.
