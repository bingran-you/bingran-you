# 2026-09-23

## Scheduled skills-freshness sweep (PR #603, merged as `11e4dadb`)

### Submodule bumps

| Submodule | Upstream tracked in `.gitmodules` | Old | New |
| --- | --- | --- | --- |
| `trusted-external-repos/gstack` | `garrytan/gstack` | `35dd014c` (v1.87.5.0) | `636175d3` (v1.87.6.0 — "make checks reliable and everyday validation faster") |
| `trusted-external-repos/open-design` | `nexu-io/open-design` | `b40f25a1c8` (v0.24.0) | `7bad1a45f2` (6 commits: telemetry classify publish/deploy failures, diagnostics evidence collection OPEND-3397, task-type menu / campaign stability, plan workload contracts, daemon heap/SQLite/OOM observability) |

Already at upstream head, untouched: `marketingskills` (`coreyhaines31/marketingskills@5b2c0007`), `mattpocock-skills` (`mattpocock/skills@c55ee460`), `claude-skills` (`bingran-you/claude-skills@34040c9c` — clean mirror of `anthropics/skills@34040c9c`), `skills` (`bingran-you/skills@49f948fa` — clean mirror of `openai/skills@49f948fa`).

`open-design` skills/ subtree was unchanged in the 6-commit range (`git diff --shortstat b40f25a1..7bad1a45 -- skills/` empty). gstack v1.87.5→v1.87.6 modified 50 SKILL.md files in place, added/removed none.

### Vendored `repo-skills/*` re-diff

Neither `claude-skills` nor `skills` submodule pointer moved this round, so no vendored `repo-skills/*` file needed refreshing (per the 2026-09-22 rule).

### Catalog / skill-files regen

`make sync` (via `zsh scripts/sync_skills.sh refresh` + `npm run skills:generate`) yielded **331 mirrored skills, 331 downloadable copies, 331 `/skills/[slug]` prerendered pages**. Same count as 2026-09-22 sweep — no adds/removes.

Artifact diff in the sync commit: `personal-site/lib/skills.generated.json` + 49 `personal-site/public/skill-files/*.md` (all gstack skills whose SKILL.md changed in v1.87.6.0). `.agents/skills/` and `.claude/skills/` entrypoint symlinks unchanged.

Followed the 2026-09-22 order-of-operations rule: (a) committed submodule bumps, (b) ran `make sync`, (c) committed regenerated artifacts, (d) re-ran `make sync` — clean tree with no timestamp-drift residual. No CI push cycle needed.

### CI / merge

`check` (sync-check gate) went green in 2m34s on the second commit (`6a13b94`). Vercel Preview Comments check ok. Marked PR ready-for-review, squash-merged as `11e4dadbdbf12b822aa0382ea1458f8e52d0a4d8` on `main`.

### Branch delete still blocked at proxy

Same 403 as 2026-09-22: `git push origin --delete claude/quirky-cori-p7nqzj` fails with `HTTP 403 curl 22` at the agent proxy. `mcp__github` MCP server exposes `create_branch` but no `delete_branch` / `delete_ref` counterpart, so the API path documented as also-blocked in the 2026-09-22 memo is not reachable from this session either. Branch remains on origin for manual cleanup.

### Environment notes

- Remote container was shallow-cloned (`git rev-parse --is-shallow-repository` → `true`); ran `git fetch --unshallow` before `make sync` for accurate `updatedAt` timestamps.
- Managed submodules cloned full-depth by `git submodule update --init` (attempting `git fetch --unshallow` on them errored `--unshallow on a complete repository does not make sense`, which is expected — they were never shallow).
- `zsh` missing on fresh container: `apt-get install -y zsh` (same as prior sweeps).
- `personal-site/node_modules` absent: `npm install` ran ~clean (npm 10.9.7; only a "New major version" notice for 12.1.0).
- Only inited the six submodules the sync actually needs (`open-design`, `marketingskills`, `gstack`, `mattpocock-skills`, `claude-skills`, `skills`) — deliberately did NOT `--recursive` init to avoid the .git/worktrees bloat described in `TOOLS.md`.
