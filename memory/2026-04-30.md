# 2026-04-30

- Audited top-level workspace docs after a repo layout refresh.
- Synced `AGENTS.md` with the current structure: added `current-projects/`, `repo-skills/`, `social-media/`, `.codex/`, the full submodule list, skill-sync notes, and more robust memory bootstrap guidance.
- Synced `IDENTITY.md`, `SOUL.md`, and `USER.md` to the current language policy: Chinese by default when replying directly to Bingran, English for system artifacts.
- Added a bootstrap `MEMORY.md` so main sessions have a curated workspace-memory file to read.
- Removed the duplicate `repo-skills/vercel-deploy copy/` directory after confirming it was the only remaining invalid skill name for mirroring.
- Verified `scripts/sync_skills.sh status`: 26 valid skills from `repo-skills/`, 35 from `trusted-external-repos/open-design/skills/`, mirrored into both `.agents/skills` and `.claude/skills` with no skipped invalid names.
