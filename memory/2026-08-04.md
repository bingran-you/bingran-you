# 2026-08-04

## CLI updates: Claude Code + Codex, Windows & WSL

Bingran asked to update all local Claude Code / Codex CLIs on both
systems to latest. State afterward:

| Environment | Claude Code | Codex | Notes |
| --- | --- | --- | --- |
| Windows (npm) | 2.1.221 (already latest) | 0.77.0 → **0.146.0** | `codex` on PATH is the npm shim; desktop-managed `codex.exe` under `AppData\Local\OpenAI\Codex\bin\` self-updates with the app |
| WSL Ubuntu 22.04 | **2.1.221 native install** (new) | **0.146.0 native install** (new) | WSL previously had NO native installs — `claude`/`codex` leaked through to the Windows npm shims via `/mnt/c` PATH interop, and codex was broken there (no Linux node) |

WSL install method (no node in WSL, so no npm):

- claude: official `curl -fsSL https://claude.ai/install.sh | bash`
  → `~/.local/bin/claude` (self-updating native build).
- codex: GitHub release asset `codex-x86_64-unknown-linux-musl.tar.gz`
  from `rust-v0.146.0` → `~/.local/bin/codex`. Careful: a naive grep for
  `x86_64-unknown-linux-musl` matches the `bwrap-*` asset first; anchor
  on `/codex-x86_64-...`.
- Both resolve ahead of the `/mnt/c` shims in login shells.

## WSL trajectories routed to H: too

Extended yesterday's H:\WorkTrees policy to WSL (H: mounts at `/mnt/h`):

- `~/.claude/projects` → symlink → `/mnt/h/WorkTrees/claude/trajectories`
- `~/.codex/sessions` → symlink → `/mnt/h/WorkTrees/codex/trajectories`
- Fresh `~/.claude/settings.json` with `"cleanupPeriodDays": 36500`.

Shared with Windows safely: Claude project dirs are path-munged (WSL
paths → `-tmp`, `-home-bingran-you-...`) and codex rollouts are
timestamp+UUID named, so no collisions.

Smoke-tested: unauthenticated `claude -p` and `codex exec` in WSL both
wrote their session files through the symlinks onto H: (auth fails,
storage path proven — same pattern as yesterday's Windows test).

**Auth still pending in WSL** (fresh homes): `claude` needs one
interactive `/login`; codex needs `codex login` or an Azure-provider
`config.toml` + `AZURE_API_KEY` like the Windows setup. Flag for
Bingran if he intends to actually work from WSL.

## Incident: OneDrive migrated the repo out from under us

While writing up the CLI work, the workspace at
`C:\Users\Bingran You\Documents\GitHub\bingran-you` turned out to be
gutted — no `.git`, most files gone. Forensics:

- The system Documents folder is redirected to `H:\OneDrive\Documents`.
  OneDrive (process up since 01:52 AM) migrated the working repo into
  `H:\OneDrive\Documents\GitHub\bingran-you` between 01:55–02:47 AM,
  leaving only in-use files behind on C:. No agent session was active
  overnight (zero trajectories on H: in that window) — this was OneDrive,
  not an agent.
- The migration target already held a synced copy of the **fresh clone
  from 21:37 the night before** (the old 1.24 GB repo had been recycled
  at 21:37 — still in the Windows Recycle Bin as a backup). The merge
  produced dozens of `* - Copy` conflict files and a stale `.git`
  (HEAD at #510) — the merged tree was untrustworthy.
- Bingran then deleted the OneDrive copy while cleaning up (local
  deletions from a sync folder go to the **OneDrive web recycle bin**,
  not the local one).

Recovery (nothing tracked was lost — origin/main had everything):

- Fresh full clone → **`H:\GitHub\bingran-you`** (new canonical home,
  outside OneDrive; Bingran chose this). `core.longpaths`/`core.symlinks`
  were already global; CLAUDE.md symlink checked out fine.
  `bingran-you-private` re-initialized.
- `memory/2026-08-04.md` carried over from the C: husk (written after
  the wipe, so it survived).
- `.env` (21 KB, created 22:00 the night before, never in git): the only
  remaining copy is in the **OneDrive web recycle bin** — Bingran must
  restore it from onedrive.live.com if needed. `AZURE_API_KEY` itself is
  safe in the Windows user environment.
- Left untouched for Bingran to dispose of after verification: the C:
  husk (contains a partial `bingran-you-private` residue — trash it once
  confirmed), the recycled 1.24 GB original, and the stale codex
  worktrees (`0d07`, `8447`) whose gitdirs pointed at the destroyed C:
  `.git`.

Follow-ups: desktop apps / codex trust list should open the repo from
`H:\GitHub\bingran-you` now; H:\WorkTrees trajectory routing was never
affected (outside OneDrive's scope).
