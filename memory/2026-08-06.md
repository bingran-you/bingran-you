# 2026-08-06

## Trajectory + worktree storage moved H:\WorkTrees → L:\Agents

Bingran asked to fully migrate agent trajectory and worktree storage
from `H:\WorkTrees` to `L:\Agents`. Executed copy → flip → delta →
verify → archive, with zero data loss and no downtime for the live
Claude session (its transcript kept appending through the junction the
whole time).

### New layout

| Link | Target |
| --- | --- |
| `~/.claude/projects` (junction) | `L:\Agents\claude\trajectories` |
| `~/.codex/sessions` (junction) | `L:\Agents\codex\trajectories` |
| `~/.codex/worktrees` (junction) | `L:\Agents\codex\worktrees` |
| WSL `~/.claude/projects` (symlink) | `/mnt/l/Agents/claude/trajectories` |
| WSL `~/.codex/sessions` (symlink) | `/mnt/l/Agents/codex/trajectories` |

Manual big-worktree convention moved too: `L:\Agents\claude\worktrees\bingran-you\<name>`.

### Gotchas hit during the copy (worth remembering)

- The codex worktrees tree (5.9 GB, 58k files) contains repo-tracked
  symlinks (`CLAUDE.md`, `.agents/.claude` skills mirrors). Plain
  robocopy **follows** symlinks and chokes on broken ones; robocopy
  `/SL` (copy links as links) fails with ERROR 1314 when not elevated —
  Developer Mode does not help robocopy. Net effect: symlinks got
  materialized or skipped. That's fine here — they're repo-derivable,
  zero unique data — so L: ended up a superset of H:'s real files
  (58,164 vs 56,911 non-reparse files).
- Bingran exited Codex desktop mid-migration so the sessions/worktrees
  junctions could flip with no open handles. Post-flip delta copies
  were all zero — nothing was written to H: after the switch.

### Verification

- Live Claude transcript (this session) appending on L: ✓
- Fresh `codex exec` rollout written to `L:\Agents\codex\trajectories\2026\08\06` ✓
- WSL symlinks resolve and list through `/mnt/l` ✓
- Retention unchanged (`cleanupPeriodDays: 36500`; codex has no GC) ✓

### Archived, delete only with Bingran's OK

- `H:\WorkTrees.migrated-to-L.2026-08-06` — the frozen pre-move tree
  (includes the two-then-three stale codex worktrees `0d07`/`8447`/`d047`
  whose gitdirs still point at the destroyed pre-OneDrive-incident C:
  `.git`; they're dead weight on both drives).
- `~/.claude/projects.pre-H-migration`, `~/.codex/sessions.pre-H-migration`,
  `~/.codex/worktrees.pre-H-migration` — the original 2026-08-03 C: copies.
